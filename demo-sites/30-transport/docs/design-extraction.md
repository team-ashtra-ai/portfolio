# Design Extraction

For every reference, extract principles rather than copying visible identity. The fixed ASH-TRA page and section matrix remains unchanged.

## 1. Citymapper (direct)

URL: https://citymapper.com/

| Extraction Field | Principle For This ASH-TRA Site |
| --- | --- |
| Layout archetype | Extract industry-specific transport, mobility & passenger services credibility; translate into Map-first route planner rather than copying Citymapper. |
| Header structure | Borrow the decision logic of a direct reference, then rebuild as Route utility header. |
| Desktop navigation style | Use clear route grouping for fixed ASH-TRA pages with density tuned to practical. |
| Mobile menu behaviour | Adapt mobile cues into Primary-reference mobile navigation with static-content fallback. |
| Footer structure | Use sitemap completeness and trust routing, rebuilt as fleet support footer. |
| Typography mood | Study the mood, then express it through Aptos display with Inter body and Constantia accent labels. |
| Colour palette | Translate reference contrast into the original palette #eaff57, #133a1b, #00a66a, #133a1b, #ffffff. |
| Surface system | Use surface hierarchy ideas only; final surfaces are Transit utility with bright green, map-first decisions, and route cards. |
| Hero type | Use the reference to pressure-test the first screen, then build an original Green transit planner hero. |
| Section rhythm | Extract pacing lessons and apply varied fixed-section composition, not a copied sequence. |
| Card design | Convert card intent into Transit route cards with unique spacing, shape, and state rules. |
| Form pattern | Use friction and reassurance cues to refine the original Fare estimate form. |
| CTA flow | Map intent to Primary-reference CTA hierarchy using Book Ride and primary CTA `Book Ride`. |
| Asset direction | Translate asset principles into Transit maps, route lines, vehicles, city movement and local ASH-TRA assets. |
| Image treatment | Use crop/contrast lessons without using the reference images, crops, or art direction directly. |
| Icon/illustration style | Extract clarity and hierarchy only; final icon language stays local to RideSure. |
| JS interaction ideas | Use as inspiration for Route selector, fare estimate, fleet filter while preserving readable static content. |
| Motion style | Reduce or reshape motion into Route line reveal with reduced-motion support. |
| Mobile behaviour | Apply mobile task priority to Primary-reference mobile navigation and responsive section rhythm. |
| What must not be copied | Do not copy code, copy, logos, images, brand assets, exact layouts, exact animations, or proprietary identity. |

## 2. Uber (direct)

URL: https://www.uber.com

| Extraction Field | Principle For This ASH-TRA Site |
| --- | --- |
| Layout archetype | Extract industry-specific transport, mobility & passenger services credibility; translate into Map-first route planner rather than copying Uber. |
| Header structure | Borrow the decision logic of a direct reference, then rebuild as Route utility header. |
| Desktop navigation style | Use clear route grouping for fixed ASH-TRA pages with density tuned to practical. |
| Mobile menu behaviour | Adapt mobile cues into Primary-reference mobile navigation with static-content fallback. |
| Footer structure | Use sitemap completeness and trust routing, rebuilt as fleet support footer. |
| Typography mood | Study the mood, then express it through Aptos display with Inter body and Constantia accent labels. |
| Colour palette | Translate reference contrast into the original palette #eaff57, #133a1b, #00a66a, #133a1b, #ffffff. |
| Surface system | Use surface hierarchy ideas only; final surfaces are Transit utility with bright green, map-first decisions, and route cards. |
| Hero type | Use the reference to pressure-test the first screen, then build an original Green transit planner hero. |
| Section rhythm | Extract pacing lessons and apply varied fixed-section composition, not a copied sequence. |
| Card design | Convert card intent into Transit route cards with unique spacing, shape, and state rules. |
| Form pattern | Use friction and reassurance cues to refine the original Fare estimate form. |
| CTA flow | Map intent to Primary-reference CTA hierarchy using Book Ride and primary CTA `Book Ride`. |
| Asset direction | Translate asset principles into Transit maps, route lines, vehicles, city movement and local ASH-TRA assets. |
| Image treatment | Use crop/contrast lessons without using the reference images, crops, or art direction directly. |
| Icon/illustration style | Extract clarity and hierarchy only; final icon language stays local to RideSure. |
| JS interaction ideas | Use as inspiration for Route selector, fare estimate, fleet filter while preserving readable static content. |
| Motion style | Reduce or reshape motion into Route line reveal with reduced-motion support. |
| Mobile behaviour | Apply mobile task priority to Primary-reference mobile navigation and responsive section rhythm. |
| What must not be copied | Do not copy code, copy, logos, images, brand assets, exact layouts, exact animations, or proprietary identity. |

## 3. Lyft (direct)

URL: https://www.lyft.com

| Extraction Field | Principle For This ASH-TRA Site |
| --- | --- |
| Layout archetype | Extract industry-specific transport, mobility & passenger services credibility; translate into Map-first route planner rather than copying Lyft. |
| Header structure | Borrow the decision logic of a direct reference, then rebuild as Route utility header. |
| Desktop navigation style | Use clear route grouping for fixed ASH-TRA pages with density tuned to practical. |
| Mobile menu behaviour | Adapt mobile cues into Primary-reference mobile navigation with static-content fallback. |
| Footer structure | Use sitemap completeness and trust routing, rebuilt as fleet support footer. |
| Typography mood | Study the mood, then express it through Aptos display with Inter body and Constantia accent labels. |
| Colour palette | Translate reference contrast into the original palette #eaff57, #133a1b, #00a66a, #133a1b, #ffffff. |
| Surface system | Use surface hierarchy ideas only; final surfaces are Transit utility with bright green, map-first decisions, and route cards. |
| Hero type | Use the reference to pressure-test the first screen, then build an original Green transit planner hero. |
| Section rhythm | Extract pacing lessons and apply varied fixed-section composition, not a copied sequence. |
| Card design | Convert card intent into Transit route cards with unique spacing, shape, and state rules. |
| Form pattern | Use friction and reassurance cues to refine the original Fare estimate form. |
| CTA flow | Map intent to Primary-reference CTA hierarchy using Book Ride and primary CTA `Book Ride`. |
| Asset direction | Translate asset principles into Transit maps, route lines, vehicles, city movement and local ASH-TRA assets. |
| Image treatment | Use crop/contrast lessons without using the reference images, crops, or art direction directly. |
| Icon/illustration style | Extract clarity and hierarchy only; final icon language stays local to RideSure. |
| JS interaction ideas | Use as inspiration for Route selector, fare estimate, fleet filter while preserving readable static content. |
| Motion style | Reduce or reshape motion into Route line reveal with reduced-motion support. |
| Mobile behaviour | Apply mobile task priority to Primary-reference mobile navigation and responsive section rhythm. |
| What must not be copied | Do not copy code, copy, logos, images, brand assets, exact layouts, exact animations, or proprietary identity. |

## 4. Bolt (direct)

URL: https://bolt.eu

| Extraction Field | Principle For This ASH-TRA Site |
| --- | --- |
| Layout archetype | Extract industry-specific transport, mobility & passenger services credibility; translate into Map-first route planner rather than copying Bolt. |
| Header structure | Borrow the decision logic of a direct reference, then rebuild as Route utility header. |
| Desktop navigation style | Use clear route grouping for fixed ASH-TRA pages with density tuned to practical. |
| Mobile menu behaviour | Adapt mobile cues into Primary-reference mobile navigation with static-content fallback. |
| Footer structure | Use sitemap completeness and trust routing, rebuilt as fleet support footer. |
| Typography mood | Study the mood, then express it through Aptos display with Inter body and Constantia accent labels. |
| Colour palette | Translate reference contrast into the original palette #eaff57, #133a1b, #00a66a, #133a1b, #ffffff. |
| Surface system | Use surface hierarchy ideas only; final surfaces are Transit utility with bright green, map-first decisions, and route cards. |
| Hero type | Use the reference to pressure-test the first screen, then build an original Green transit planner hero. |
| Section rhythm | Extract pacing lessons and apply varied fixed-section composition, not a copied sequence. |
| Card design | Convert card intent into Transit route cards with unique spacing, shape, and state rules. |
| Form pattern | Use friction and reassurance cues to refine the original Fare estimate form. |
| CTA flow | Map intent to Primary-reference CTA hierarchy using Book Ride and primary CTA `Book Ride`. |
| Asset direction | Translate asset principles into Transit maps, route lines, vehicles, city movement and local ASH-TRA assets. |
| Image treatment | Use crop/contrast lessons without using the reference images, crops, or art direction directly. |
| Icon/illustration style | Extract clarity and hierarchy only; final icon language stays local to RideSure. |
| JS interaction ideas | Use as inspiration for Route selector, fare estimate, fleet filter while preserving readable static content. |
| Motion style | Reduce or reshape motion into Route line reveal with reduced-motion support. |
| Mobile behaviour | Apply mobile task priority to Primary-reference mobile navigation and responsive section rhythm. |
| What must not be copied | Do not copy code, copy, logos, images, brand assets, exact layouts, exact animations, or proprietary identity. |

## 5. Citymapper (adjacent)

URL: https://citymapper.com

| Extraction Field | Principle For This ASH-TRA Site |
| --- | --- |
| Layout archetype | Extract adjacent-category translation for safe reliable movement; translate into Map-first route planner rather than copying Citymapper. |
| Header structure | Borrow the decision logic of a adjacent reference, then rebuild as Route utility header. |
| Desktop navigation style | Use clear route grouping for fixed ASH-TRA pages with density tuned to practical. |
| Mobile menu behaviour | Adapt mobile cues into Primary-reference mobile navigation with static-content fallback. |
| Footer structure | Use sitemap completeness and trust routing, rebuilt as fleet support footer. |
| Typography mood | Study the mood, then express it through Aptos display with Inter body and Constantia accent labels. |
| Colour palette | Translate reference contrast into the original palette #eaff57, #133a1b, #00a66a, #133a1b, #ffffff. |
| Surface system | Use surface hierarchy ideas only; final surfaces are Transit utility with bright green, map-first decisions, and route cards. |
| Hero type | Use the reference to pressure-test the first screen, then build an original Green transit planner hero. |
| Section rhythm | Extract pacing lessons and apply varied fixed-section composition, not a copied sequence. |
| Card design | Convert card intent into Transit route cards with unique spacing, shape, and state rules. |
| Form pattern | Use friction and reassurance cues to refine the original Fare estimate form. |
| CTA flow | Map intent to Primary-reference CTA hierarchy using Book Ride and primary CTA `Book Ride`. |
| Asset direction | Translate asset principles into Transit maps, route lines, vehicles, city movement and local ASH-TRA assets. |
| Image treatment | Use crop/contrast lessons without using the reference images, crops, or art direction directly. |
| Icon/illustration style | Extract clarity and hierarchy only; final icon language stays local to RideSure. |
| JS interaction ideas | Use as inspiration for Route selector, fare estimate, fleet filter while preserving readable static content. |
| Motion style | Reduce or reshape motion into Route line reveal with reduced-motion support. |
| Mobile behaviour | Apply mobile task priority to Primary-reference mobile navigation and responsive section rhythm. |
| What must not be copied | Do not copy code, copy, logos, images, brand assets, exact layouts, exact animations, or proprietary identity. |

## 6. Trainline (adjacent)

URL: https://www.thetrainline.com

| Extraction Field | Principle For This ASH-TRA Site |
| --- | --- |
| Layout archetype | Extract adjacent-category translation for safe reliable movement; translate into Map-first route planner rather than copying Trainline. |
| Header structure | Borrow the decision logic of a adjacent reference, then rebuild as Route utility header. |
| Desktop navigation style | Use clear route grouping for fixed ASH-TRA pages with density tuned to practical. |
| Mobile menu behaviour | Adapt mobile cues into Primary-reference mobile navigation with static-content fallback. |
| Footer structure | Use sitemap completeness and trust routing, rebuilt as fleet support footer. |
| Typography mood | Study the mood, then express it through Aptos display with Inter body and Constantia accent labels. |
| Colour palette | Translate reference contrast into the original palette #eaff57, #133a1b, #00a66a, #133a1b, #ffffff. |
| Surface system | Use surface hierarchy ideas only; final surfaces are Transit utility with bright green, map-first decisions, and route cards. |
| Hero type | Use the reference to pressure-test the first screen, then build an original Green transit planner hero. |
| Section rhythm | Extract pacing lessons and apply varied fixed-section composition, not a copied sequence. |
| Card design | Convert card intent into Transit route cards with unique spacing, shape, and state rules. |
| Form pattern | Use friction and reassurance cues to refine the original Fare estimate form. |
| CTA flow | Map intent to Primary-reference CTA hierarchy using Book Ride and primary CTA `Book Ride`. |
| Asset direction | Translate asset principles into Transit maps, route lines, vehicles, city movement and local ASH-TRA assets. |
| Image treatment | Use crop/contrast lessons without using the reference images, crops, or art direction directly. |
| Icon/illustration style | Extract clarity and hierarchy only; final icon language stays local to RideSure. |
| JS interaction ideas | Use as inspiration for Route selector, fare estimate, fleet filter while preserving readable static content. |
| Motion style | Reduce or reshape motion into Route line reveal with reduced-motion support. |
| Mobile behaviour | Apply mobile task priority to Primary-reference mobile navigation and responsive section rhythm. |
| What must not be copied | Do not copy code, copy, logos, images, brand assets, exact layouts, exact animations, or proprietary identity. |

## 7. Starlink (contrast)

URL: https://www.starlink.com

| Extraction Field | Principle For This ASH-TRA Site |
| --- | --- |
| Layout archetype | Extract contrast-world inspiration to avoid generic professional design; translate into Map-first route planner rather than copying Starlink. |
| Header structure | Borrow the decision logic of a contrast reference, then rebuild as Route utility header. |
| Desktop navigation style | Use clear route grouping for fixed ASH-TRA pages with density tuned to practical. |
| Mobile menu behaviour | Adapt mobile cues into Primary-reference mobile navigation with static-content fallback. |
| Footer structure | Use sitemap completeness and trust routing, rebuilt as fleet support footer. |
| Typography mood | Study the mood, then express it through Aptos display with Inter body and Constantia accent labels. |
| Colour palette | Translate reference contrast into the original palette #eaff57, #133a1b, #00a66a, #133a1b, #ffffff. |
| Surface system | Use surface hierarchy ideas only; final surfaces are Transit utility with bright green, map-first decisions, and route cards. |
| Hero type | Use the reference to pressure-test the first screen, then build an original Green transit planner hero. |
| Section rhythm | Extract pacing lessons and apply varied fixed-section composition, not a copied sequence. |
| Card design | Convert card intent into Transit route cards with unique spacing, shape, and state rules. |
| Form pattern | Use friction and reassurance cues to refine the original Fare estimate form. |
| CTA flow | Map intent to Primary-reference CTA hierarchy using Book Ride and primary CTA `Book Ride`. |
| Asset direction | Translate asset principles into Transit maps, route lines, vehicles, city movement and local ASH-TRA assets. |
| Image treatment | Use crop/contrast lessons without using the reference images, crops, or art direction directly. |
| Icon/illustration style | Extract clarity and hierarchy only; final icon language stays local to RideSure. |
| JS interaction ideas | Use as inspiration for Route selector, fare estimate, fleet filter while preserving readable static content. |
| Motion style | Reduce or reshape motion into Route line reveal with reduced-motion support. |
| Mobile behaviour | Apply mobile task priority to Primary-reference mobile navigation and responsive section rhythm. |
| What must not be copied | Do not copy code, copy, logos, images, brand assets, exact layouts, exact animations, or proprietary identity. |

## 8. Google Maps (contrast)

URL: https://www.google.com/maps

| Extraction Field | Principle For This ASH-TRA Site |
| --- | --- |
| Layout archetype | Extract contrast-world inspiration to avoid generic professional design; translate into Map-first route planner rather than copying Google Maps. |
| Header structure | Borrow the decision logic of a contrast reference, then rebuild as Route utility header. |
| Desktop navigation style | Use clear route grouping for fixed ASH-TRA pages with density tuned to practical. |
| Mobile menu behaviour | Adapt mobile cues into Primary-reference mobile navigation with static-content fallback. |
| Footer structure | Use sitemap completeness and trust routing, rebuilt as fleet support footer. |
| Typography mood | Study the mood, then express it through Aptos display with Inter body and Constantia accent labels. |
| Colour palette | Translate reference contrast into the original palette #eaff57, #133a1b, #00a66a, #133a1b, #ffffff. |
| Surface system | Use surface hierarchy ideas only; final surfaces are Transit utility with bright green, map-first decisions, and route cards. |
| Hero type | Use the reference to pressure-test the first screen, then build an original Green transit planner hero. |
| Section rhythm | Extract pacing lessons and apply varied fixed-section composition, not a copied sequence. |
| Card design | Convert card intent into Transit route cards with unique spacing, shape, and state rules. |
| Form pattern | Use friction and reassurance cues to refine the original Fare estimate form. |
| CTA flow | Map intent to Primary-reference CTA hierarchy using Book Ride and primary CTA `Book Ride`. |
| Asset direction | Translate asset principles into Transit maps, route lines, vehicles, city movement and local ASH-TRA assets. |
| Image treatment | Use crop/contrast lessons without using the reference images, crops, or art direction directly. |
| Icon/illustration style | Extract clarity and hierarchy only; final icon language stays local to RideSure. |
| JS interaction ideas | Use as inspiration for Route selector, fare estimate, fleet filter while preserving readable static content. |
| Motion style | Reduce or reshape motion into Route line reveal with reduced-motion support. |
| Mobile behaviour | Apply mobile task priority to Primary-reference mobile navigation and responsive section rhythm. |
| What must not be copied | Do not copy code, copy, logos, images, brand assets, exact layouts, exact animations, or proprietary identity. |

## 9. Mobbin route selector patterns (interaction)

URL: https://mobbin.com

| Extraction Field | Principle For This ASH-TRA Site |
| --- | --- |
| Layout archetype | Extract interaction reference for Route selector, fare estimate, fleet filter; translate into Map-first route planner rather than copying Mobbin route selector patterns. |
| Header structure | Borrow the decision logic of a interaction reference, then rebuild as Route utility header. |
| Desktop navigation style | Use clear route grouping for fixed ASH-TRA pages with density tuned to practical. |
| Mobile menu behaviour | Adapt mobile cues into Primary-reference mobile navigation with static-content fallback. |
| Footer structure | Use sitemap completeness and trust routing, rebuilt as fleet support footer. |
| Typography mood | Study the mood, then express it through Aptos display with Inter body and Constantia accent labels. |
| Colour palette | Translate reference contrast into the original palette #eaff57, #133a1b, #00a66a, #133a1b, #ffffff. |
| Surface system | Use surface hierarchy ideas only; final surfaces are Transit utility with bright green, map-first decisions, and route cards. |
| Hero type | Use the reference to pressure-test the first screen, then build an original Green transit planner hero. |
| Section rhythm | Extract pacing lessons and apply varied fixed-section composition, not a copied sequence. |
| Card design | Convert card intent into Transit route cards with unique spacing, shape, and state rules. |
| Form pattern | Use friction and reassurance cues to refine the original Fare estimate form. |
| CTA flow | Map intent to Primary-reference CTA hierarchy using Book Ride and primary CTA `Book Ride`. |
| Asset direction | Translate asset principles into Transit maps, route lines, vehicles, city movement and local ASH-TRA assets. |
| Image treatment | Use crop/contrast lessons without using the reference images, crops, or art direction directly. |
| Icon/illustration style | Extract clarity and hierarchy only; final icon language stays local to RideSure. |
| JS interaction ideas | Use as inspiration for Route selector, fare estimate, fleet filter while preserving readable static content. |
| Motion style | Reduce or reshape motion into Route line reveal with reduced-motion support. |
| Mobile behaviour | Apply mobile task priority to Primary-reference mobile navigation and responsive section rhythm. |
| What must not be copied | Do not copy code, copy, logos, images, brand assets, exact layouts, exact animations, or proprietary identity. |

## Originality Commitment

Inspired by these patterns, this ASH-TRA site will use an original design system with different branding, copy, layout, CSS, assets, and interactions.

## Static Authorship Pass

This site folder is now treated as the editable static source for `RideSure`. The duplicate secondary header route strip was removed from the static HTML, and the footer markup/CSS was rewritten directly in the site files using the `product` footer pattern for the `Citymapper` inspiration direction. Future edits should happen in this site folder rather than regenerating shared templates.

## Portfolio Section Component Pass - 2026-05-11

RideSure now treats every main-body section as a static, WordPress-ready component with a component ID, section type, layout variation, pattern family, mobile stacking rule, and editable field map. The pass keeps the Citymapper inspiration as a principle reference only while using original local markup, CSS, JS, and assets. Repeated section names are deliberately varied so Hero, Services, Process, Results, FAQ, Contact, and CTA patterns do not collapse into one shared layout.


## Homepage Atlas Pass - 2026-05-12

- Homepage content now remains hero-first and CTA-final; the notes/disclaimer block is placed before the final CTA so the page closes on `Book Ride`.
- The hero secondary CTA scrolls to `#site-atlas`, and the atlas links every core page plus support, legal, sitemap, recovery, and asset-system routes.
- Homepage section text links now route deeper into the site instead of looping back to Home.
- Atlas variant: `private` for `Ride`, shaped by `Map-first route planner`, `Transit route cards`, `Route selector, fare estimate, fleet filter`, and `professional` mobile behaviour.
- No new external inspiration was added; the existing reference pack remains the source for layout, header, navigation, footer, typography, colour, cards, forms, assets, motion, mobile behaviour, and non-copy rules.


## Portfolio Visual QA Pass - 2026-05-12

- Section copy was tightened so reusable blocks explain `Ride` with clearer role, proof, fit, and next-step language instead of repeated placeholder patterns.
- Generic card labels were replaced with section-purpose labels across services, trust, results, editorial, FAQ, and CTA blocks.
- CSS now keeps screenshot and no-JS states visible, disables problematic `content-visibility` reservations for portfolio review, prevents horizontal overflow, and avoids awkward mid-word breaks.
- The pass preserves each site's existing inspiration pack, local assets, component families, section rhythm, and cross-site identity while improving presentation quality across all pages.
