# Design Extraction

For every reference, extract principles rather than copying visible identity. The fixed ASH-TRA page and section matrix remains unchanged.

## 1. Luma (direct)

URL: https://luma.com/

| Extraction Field | Principle For This ASH-TRA Site |
| --- | --- |
| Layout archetype | Extract industry-specific events, weddings & experience production credibility; translate into Clean event calendar system rather than copying Luma. |
| Header structure | Borrow the decision logic of a direct reference, then rebuild as Calendar utility header. |
| Desktop navigation style | Use clear route grouping for fixed ASH-TRA pages with density tuned to image-rich. |
| Mobile menu behaviour | Adapt mobile cues into Primary-reference mobile navigation with static-content fallback. |
| Footer structure | Use sitemap completeness and trust routing, rebuilt as venue and supplier footer. |
| Typography mood | Study the mood, then express it through "Arial Narrow" display with Arial body and "Segoe UI" accent labels. |
| Colour palette | Translate reference contrast into the original palette #f7f8ff, #6c47ff, #14b8a6, #111827, #ffb703. |
| Surface system | Use surface hierarchy ideas only; final surfaces are Events with clean cards, soft gradients, calendar utility, and quick RSVP routes. |
| Hero type | Use the reference to pressure-test the first screen, then build an original Event card hero. |
| Section rhythm | Extract pacing lessons and apply varied fixed-section composition, not a copied sequence. |
| Card design | Convert card intent into RSVP event cards with unique spacing, shape, and state rules. |
| Form pattern | Use friction and reassurance cues to refine the original Date enquiry form. |
| CTA flow | Map intent to Primary-reference CTA hierarchy using Enquire Date and primary CTA `Enquire Date`. |
| Asset direction | Translate asset principles into Event cards, calendars, guest lists, venue details and local ASH-TRA assets. |
| Image treatment | Use crop/contrast lessons without using the reference images, crops, or art direction directly. |
| Icon/illustration style | Extract clarity and hierarchy only; final icon language stays local to VowVenue. |
| JS interaction ideas | Use as inspiration for Event type selector, venue filter, package calculator while preserving readable static content. |
| Motion style | Reduce or reshape motion into Calendar card reveal with reduced-motion support. |
| Mobile behaviour | Apply mobile task priority to Primary-reference mobile navigation and responsive section rhythm. |
| What must not be copied | Do not copy code, copy, logos, images, brand assets, exact layouts, exact animations, or proprietary identity. |

## 2. Eventbrite (direct)

URL: https://www.eventbrite.com

| Extraction Field | Principle For This ASH-TRA Site |
| --- | --- |
| Layout archetype | Extract industry-specific events, weddings & experience production credibility; translate into Clean event calendar system rather than copying Eventbrite. |
| Header structure | Borrow the decision logic of a direct reference, then rebuild as Calendar utility header. |
| Desktop navigation style | Use clear route grouping for fixed ASH-TRA pages with density tuned to image-rich. |
| Mobile menu behaviour | Adapt mobile cues into Primary-reference mobile navigation with static-content fallback. |
| Footer structure | Use sitemap completeness and trust routing, rebuilt as venue and supplier footer. |
| Typography mood | Study the mood, then express it through "Arial Narrow" display with Arial body and "Segoe UI" accent labels. |
| Colour palette | Translate reference contrast into the original palette #f7f8ff, #6c47ff, #14b8a6, #111827, #ffb703. |
| Surface system | Use surface hierarchy ideas only; final surfaces are Events with clean cards, soft gradients, calendar utility, and quick RSVP routes. |
| Hero type | Use the reference to pressure-test the first screen, then build an original Event card hero. |
| Section rhythm | Extract pacing lessons and apply varied fixed-section composition, not a copied sequence. |
| Card design | Convert card intent into RSVP event cards with unique spacing, shape, and state rules. |
| Form pattern | Use friction and reassurance cues to refine the original Date enquiry form. |
| CTA flow | Map intent to Primary-reference CTA hierarchy using Enquire Date and primary CTA `Enquire Date`. |
| Asset direction | Translate asset principles into Event cards, calendars, guest lists, venue details and local ASH-TRA assets. |
| Image treatment | Use crop/contrast lessons without using the reference images, crops, or art direction directly. |
| Icon/illustration style | Extract clarity and hierarchy only; final icon language stays local to VowVenue. |
| JS interaction ideas | Use as inspiration for Event type selector, venue filter, package calculator while preserving readable static content. |
| Motion style | Reduce or reshape motion into Calendar card reveal with reduced-motion support. |
| Mobile behaviour | Apply mobile task priority to Primary-reference mobile navigation and responsive section rhythm. |
| What must not be copied | Do not copy code, copy, logos, images, brand assets, exact layouts, exact animations, or proprietary identity. |

## 3. The Knot (direct)

URL: https://www.theknot.com

| Extraction Field | Principle For This ASH-TRA Site |
| --- | --- |
| Layout archetype | Extract industry-specific events, weddings & experience production credibility; translate into Clean event calendar system rather than copying The Knot. |
| Header structure | Borrow the decision logic of a direct reference, then rebuild as Calendar utility header. |
| Desktop navigation style | Use clear route grouping for fixed ASH-TRA pages with density tuned to image-rich. |
| Mobile menu behaviour | Adapt mobile cues into Primary-reference mobile navigation with static-content fallback. |
| Footer structure | Use sitemap completeness and trust routing, rebuilt as venue and supplier footer. |
| Typography mood | Study the mood, then express it through "Arial Narrow" display with Arial body and "Segoe UI" accent labels. |
| Colour palette | Translate reference contrast into the original palette #f7f8ff, #6c47ff, #14b8a6, #111827, #ffb703. |
| Surface system | Use surface hierarchy ideas only; final surfaces are Events with clean cards, soft gradients, calendar utility, and quick RSVP routes. |
| Hero type | Use the reference to pressure-test the first screen, then build an original Event card hero. |
| Section rhythm | Extract pacing lessons and apply varied fixed-section composition, not a copied sequence. |
| Card design | Convert card intent into RSVP event cards with unique spacing, shape, and state rules. |
| Form pattern | Use friction and reassurance cues to refine the original Date enquiry form. |
| CTA flow | Map intent to Primary-reference CTA hierarchy using Enquire Date and primary CTA `Enquire Date`. |
| Asset direction | Translate asset principles into Event cards, calendars, guest lists, venue details and local ASH-TRA assets. |
| Image treatment | Use crop/contrast lessons without using the reference images, crops, or art direction directly. |
| Icon/illustration style | Extract clarity and hierarchy only; final icon language stays local to VowVenue. |
| JS interaction ideas | Use as inspiration for Event type selector, venue filter, package calculator while preserving readable static content. |
| Motion style | Reduce or reshape motion into Calendar card reveal with reduced-motion support. |
| Mobile behaviour | Apply mobile task priority to Primary-reference mobile navigation and responsive section rhythm. |
| What must not be copied | Do not copy code, copy, logos, images, brand assets, exact layouts, exact animations, or proprietary identity. |

## 4. Cvent (adjacent)

URL: https://www.cvent.com

| Extraction Field | Principle For This ASH-TRA Site |
| --- | --- |
| Layout archetype | Extract adjacent-category translation for emotion and confidence; translate into Clean event calendar system rather than copying Cvent. |
| Header structure | Borrow the decision logic of a adjacent reference, then rebuild as Calendar utility header. |
| Desktop navigation style | Use clear route grouping for fixed ASH-TRA pages with density tuned to image-rich. |
| Mobile menu behaviour | Adapt mobile cues into Primary-reference mobile navigation with static-content fallback. |
| Footer structure | Use sitemap completeness and trust routing, rebuilt as venue and supplier footer. |
| Typography mood | Study the mood, then express it through "Arial Narrow" display with Arial body and "Segoe UI" accent labels. |
| Colour palette | Translate reference contrast into the original palette #f7f8ff, #6c47ff, #14b8a6, #111827, #ffb703. |
| Surface system | Use surface hierarchy ideas only; final surfaces are Events with clean cards, soft gradients, calendar utility, and quick RSVP routes. |
| Hero type | Use the reference to pressure-test the first screen, then build an original Event card hero. |
| Section rhythm | Extract pacing lessons and apply varied fixed-section composition, not a copied sequence. |
| Card design | Convert card intent into RSVP event cards with unique spacing, shape, and state rules. |
| Form pattern | Use friction and reassurance cues to refine the original Date enquiry form. |
| CTA flow | Map intent to Primary-reference CTA hierarchy using Enquire Date and primary CTA `Enquire Date`. |
| Asset direction | Translate asset principles into Event cards, calendars, guest lists, venue details and local ASH-TRA assets. |
| Image treatment | Use crop/contrast lessons without using the reference images, crops, or art direction directly. |
| Icon/illustration style | Extract clarity and hierarchy only; final icon language stays local to VowVenue. |
| JS interaction ideas | Use as inspiration for Event type selector, venue filter, package calculator while preserving readable static content. |
| Motion style | Reduce or reshape motion into Calendar card reveal with reduced-motion support. |
| Mobile behaviour | Apply mobile task priority to Primary-reference mobile navigation and responsive section rhythm. |
| What must not be copied | Do not copy code, copy, logos, images, brand assets, exact layouts, exact animations, or proprietary identity. |

## 5. Vogue Weddings (adjacent)

URL: https://www.vogue.com/weddings

| Extraction Field | Principle For This ASH-TRA Site |
| --- | --- |
| Layout archetype | Extract adjacent-category translation for emotion and confidence; translate into Clean event calendar system rather than copying Vogue Weddings. |
| Header structure | Borrow the decision logic of a adjacent reference, then rebuild as Calendar utility header. |
| Desktop navigation style | Use clear route grouping for fixed ASH-TRA pages with density tuned to image-rich. |
| Mobile menu behaviour | Adapt mobile cues into Primary-reference mobile navigation with static-content fallback. |
| Footer structure | Use sitemap completeness and trust routing, rebuilt as venue and supplier footer. |
| Typography mood | Study the mood, then express it through "Arial Narrow" display with Arial body and "Segoe UI" accent labels. |
| Colour palette | Translate reference contrast into the original palette #f7f8ff, #6c47ff, #14b8a6, #111827, #ffb703. |
| Surface system | Use surface hierarchy ideas only; final surfaces are Events with clean cards, soft gradients, calendar utility, and quick RSVP routes. |
| Hero type | Use the reference to pressure-test the first screen, then build an original Event card hero. |
| Section rhythm | Extract pacing lessons and apply varied fixed-section composition, not a copied sequence. |
| Card design | Convert card intent into RSVP event cards with unique spacing, shape, and state rules. |
| Form pattern | Use friction and reassurance cues to refine the original Date enquiry form. |
| CTA flow | Map intent to Primary-reference CTA hierarchy using Enquire Date and primary CTA `Enquire Date`. |
| Asset direction | Translate asset principles into Event cards, calendars, guest lists, venue details and local ASH-TRA assets. |
| Image treatment | Use crop/contrast lessons without using the reference images, crops, or art direction directly. |
| Icon/illustration style | Extract clarity and hierarchy only; final icon language stays local to VowVenue. |
| JS interaction ideas | Use as inspiration for Event type selector, venue filter, package calculator while preserving readable static content. |
| Motion style | Reduce or reshape motion into Calendar card reveal with reduced-motion support. |
| Mobile behaviour | Apply mobile task priority to Primary-reference mobile navigation and responsive section rhythm. |
| What must not be copied | Do not copy code, copy, logos, images, brand assets, exact layouts, exact animations, or proprietary identity. |

## 6. Aman (contrast)

URL: https://www.aman.com

| Extraction Field | Principle For This ASH-TRA Site |
| --- | --- |
| Layout archetype | Extract contrast-world inspiration to avoid generic hospitality design; translate into Clean event calendar system rather than copying Aman. |
| Header structure | Borrow the decision logic of a contrast reference, then rebuild as Calendar utility header. |
| Desktop navigation style | Use clear route grouping for fixed ASH-TRA pages with density tuned to image-rich. |
| Mobile menu behaviour | Adapt mobile cues into Primary-reference mobile navigation with static-content fallback. |
| Footer structure | Use sitemap completeness and trust routing, rebuilt as venue and supplier footer. |
| Typography mood | Study the mood, then express it through "Arial Narrow" display with Arial body and "Segoe UI" accent labels. |
| Colour palette | Translate reference contrast into the original palette #f7f8ff, #6c47ff, #14b8a6, #111827, #ffb703. |
| Surface system | Use surface hierarchy ideas only; final surfaces are Events with clean cards, soft gradients, calendar utility, and quick RSVP routes. |
| Hero type | Use the reference to pressure-test the first screen, then build an original Event card hero. |
| Section rhythm | Extract pacing lessons and apply varied fixed-section composition, not a copied sequence. |
| Card design | Convert card intent into RSVP event cards with unique spacing, shape, and state rules. |
| Form pattern | Use friction and reassurance cues to refine the original Date enquiry form. |
| CTA flow | Map intent to Primary-reference CTA hierarchy using Enquire Date and primary CTA `Enquire Date`. |
| Asset direction | Translate asset principles into Event cards, calendars, guest lists, venue details and local ASH-TRA assets. |
| Image treatment | Use crop/contrast lessons without using the reference images, crops, or art direction directly. |
| Icon/illustration style | Extract clarity and hierarchy only; final icon language stays local to VowVenue. |
| JS interaction ideas | Use as inspiration for Event type selector, venue filter, package calculator while preserving readable static content. |
| Motion style | Reduce or reshape motion into Calendar card reveal with reduced-motion support. |
| Mobile behaviour | Apply mobile task priority to Primary-reference mobile navigation and responsive section rhythm. |
| What must not be copied | Do not copy code, copy, logos, images, brand assets, exact layouts, exact animations, or proprietary identity. |

## 7. Aesop (contrast)

URL: https://www.aesop.com

| Extraction Field | Principle For This ASH-TRA Site |
| --- | --- |
| Layout archetype | Extract contrast-world inspiration to avoid generic hospitality design; translate into Clean event calendar system rather than copying Aesop. |
| Header structure | Borrow the decision logic of a contrast reference, then rebuild as Calendar utility header. |
| Desktop navigation style | Use clear route grouping for fixed ASH-TRA pages with density tuned to image-rich. |
| Mobile menu behaviour | Adapt mobile cues into Primary-reference mobile navigation with static-content fallback. |
| Footer structure | Use sitemap completeness and trust routing, rebuilt as venue and supplier footer. |
| Typography mood | Study the mood, then express it through "Arial Narrow" display with Arial body and "Segoe UI" accent labels. |
| Colour palette | Translate reference contrast into the original palette #f7f8ff, #6c47ff, #14b8a6, #111827, #ffb703. |
| Surface system | Use surface hierarchy ideas only; final surfaces are Events with clean cards, soft gradients, calendar utility, and quick RSVP routes. |
| Hero type | Use the reference to pressure-test the first screen, then build an original Event card hero. |
| Section rhythm | Extract pacing lessons and apply varied fixed-section composition, not a copied sequence. |
| Card design | Convert card intent into RSVP event cards with unique spacing, shape, and state rules. |
| Form pattern | Use friction and reassurance cues to refine the original Date enquiry form. |
| CTA flow | Map intent to Primary-reference CTA hierarchy using Enquire Date and primary CTA `Enquire Date`. |
| Asset direction | Translate asset principles into Event cards, calendars, guest lists, venue details and local ASH-TRA assets. |
| Image treatment | Use crop/contrast lessons without using the reference images, crops, or art direction directly. |
| Icon/illustration style | Extract clarity and hierarchy only; final icon language stays local to VowVenue. |
| JS interaction ideas | Use as inspiration for Event type selector, venue filter, package calculator while preserving readable static content. |
| Motion style | Reduce or reshape motion into Calendar card reveal with reduced-motion support. |
| Mobile behaviour | Apply mobile task priority to Primary-reference mobile navigation and responsive section rhythm. |
| What must not be copied | Do not copy code, copy, logos, images, brand assets, exact layouts, exact animations, or proprietary identity. |

## 8. Mobbin event planner patterns (interaction)

URL: https://mobbin.com

| Extraction Field | Principle For This ASH-TRA Site |
| --- | --- |
| Layout archetype | Extract interaction reference for Event type selector, venue filter, package calculator; translate into Clean event calendar system rather than copying Mobbin event planner patterns. |
| Header structure | Borrow the decision logic of a interaction reference, then rebuild as Calendar utility header. |
| Desktop navigation style | Use clear route grouping for fixed ASH-TRA pages with density tuned to image-rich. |
| Mobile menu behaviour | Adapt mobile cues into Primary-reference mobile navigation with static-content fallback. |
| Footer structure | Use sitemap completeness and trust routing, rebuilt as venue and supplier footer. |
| Typography mood | Study the mood, then express it through "Arial Narrow" display with Arial body and "Segoe UI" accent labels. |
| Colour palette | Translate reference contrast into the original palette #f7f8ff, #6c47ff, #14b8a6, #111827, #ffb703. |
| Surface system | Use surface hierarchy ideas only; final surfaces are Events with clean cards, soft gradients, calendar utility, and quick RSVP routes. |
| Hero type | Use the reference to pressure-test the first screen, then build an original Event card hero. |
| Section rhythm | Extract pacing lessons and apply varied fixed-section composition, not a copied sequence. |
| Card design | Convert card intent into RSVP event cards with unique spacing, shape, and state rules. |
| Form pattern | Use friction and reassurance cues to refine the original Date enquiry form. |
| CTA flow | Map intent to Primary-reference CTA hierarchy using Enquire Date and primary CTA `Enquire Date`. |
| Asset direction | Translate asset principles into Event cards, calendars, guest lists, venue details and local ASH-TRA assets. |
| Image treatment | Use crop/contrast lessons without using the reference images, crops, or art direction directly. |
| Icon/illustration style | Extract clarity and hierarchy only; final icon language stays local to VowVenue. |
| JS interaction ideas | Use as inspiration for Event type selector, venue filter, package calculator while preserving readable static content. |
| Motion style | Reduce or reshape motion into Calendar card reveal with reduced-motion support. |
| Mobile behaviour | Apply mobile task priority to Primary-reference mobile navigation and responsive section rhythm. |
| What must not be copied | Do not copy code, copy, logos, images, brand assets, exact layouts, exact animations, or proprietary identity. |

## Originality Commitment

Inspired by these patterns, this ASH-TRA site will use an original design system with different branding, copy, layout, CSS, assets, and interactions.

## Static Authorship Pass

This site folder is now treated as the editable static source for `VowVenue`. The duplicate secondary header route strip was removed from the static HTML, and the footer markup/CSS was rewritten directly in the site files using the `immersive` footer pattern for the `Luma` inspiration direction. Future edits should happen in this site folder rather than regenerating shared templates.

## Portfolio Section Component Pass - 2026-05-11

VowVenue now treats every main-body section as a static, WordPress-ready component with a component ID, section type, layout variation, pattern family, mobile stacking rule, and editable field map. The pass keeps the Luma inspiration as a principle reference only while using original local markup, CSS, JS, and assets. Repeated section names are deliberately varied so Hero, Services, Process, Results, FAQ, Contact, and CTA patterns do not collapse into one shared layout.


## Homepage Atlas Pass - 2026-05-12

- Homepage content now remains hero-first and CTA-final; the notes/disclaimer block is placed before the final CTA so the page closes on `Enquire Date`.
- The hero secondary CTA scrolls to `#site-atlas`, and the atlas links every core page plus support, legal, sitemap, recovery, and asset-system routes.
- Homepage section text links now route deeper into the site instead of looping back to Home.
- Atlas variant: `magazine` for `Vow`, shaped by `Clean event calendar system`, `RSVP event cards`, `Event type selector, venue filter, package calculator`, and `hospitality` mobile behaviour.
- No new external inspiration was added; the existing reference pack remains the source for layout, header, navigation, footer, typography, colour, cards, forms, assets, motion, mobile behaviour, and non-copy rules.


## Portfolio Visual QA Pass - 2026-05-12

- Section copy was tightened so reusable blocks explain `Vow` with clearer role, proof, fit, and next-step language instead of repeated placeholder patterns.
- Generic card labels were replaced with section-purpose labels across services, trust, results, editorial, FAQ, and CTA blocks.
- CSS now keeps screenshot and no-JS states visible, disables problematic `content-visibility` reservations for portfolio review, prevents horizontal overflow, and avoids awkward mid-word breaks.
- The pass preserves each site's existing inspiration pack, local assets, component families, section rhythm, and cross-site identity while improving presentation quality across all pages.
