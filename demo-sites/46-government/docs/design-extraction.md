# Design Extraction

For every reference, extract principles rather than copying visible identity. The fixed ASH-TRA page and section matrix remains unchanged.

## 1. GOV.UK Design System (direct)

URL: https://design-system.service.gov.uk/

| Extraction Field | Principle For This ASH-TRA Site |
| --- | --- |
| Layout archetype | Extract industry-specific government, public sector & civic services credibility; translate into Public service task flow rather than copying GOV.UK Design System. |
| Header structure | Borrow the decision logic of a direct reference, then rebuild as GOV-style service header. |
| Desktop navigation style | Use clear route grouping for fixed ASH-TRA pages with density tuned to practical. |
| Mobile menu behaviour | Adapt mobile cues into Primary-reference mobile navigation with static-content fallback. |
| Footer structure | Use sitemap completeness and trust routing, rebuilt as service directory footer. |
| Typography mood | Study the mood, then express it through "Arial Narrow" display with "Segoe UI" body and "Lucida Sans" accent labels. |
| Colour palette | Translate reference contrast into the original palette #ffffff, #1d70b8, #ffdd00, #0b0c0c, #ffdd00. |
| Surface system | Use surface hierarchy ideas only; final surfaces are Public service with plain language, blue actions, and high-contrast forms. |
| Hero type | Use the reference to pressure-test the first screen, then build an original Plain service hero. |
| Section rhythm | Extract pacing lessons and apply varied fixed-section composition, not a copied sequence. |
| Card design | Convert card intent into Square service boxes with unique spacing, shape, and state rules. |
| Form pattern | Use friction and reassurance cues to refine the original Public request form. |
| CTA flow | Map intent to Primary-reference CTA hierarchy using Start Request and primary CTA `Start Request`. |
| Asset direction | Translate asset principles into Forms, service lists, notices, public task flows and local ASH-TRA assets. |
| Image treatment | Use crop/contrast lessons without using the reference images, crops, or art direction directly. |
| Icon/illustration style | Extract clarity and hierarchy only; final icon language stays local to CivicAccess. |
| JS interaction ideas | Use as inspiration for Service search, form finder, document filter, alert controls while preserving readable static content. |
| Motion style | Reduce or reshape motion into Minimal state reveal with reduced-motion support. |
| Mobile behaviour | Apply mobile task priority to Primary-reference mobile navigation and responsive section rhythm. |
| What must not be copied | Do not copy code, copy, logos, images, brand assets, exact layouts, exact animations, or proprietary identity. |

## 2. GOV.UK (direct)

URL: https://www.gov.uk

| Extraction Field | Principle For This ASH-TRA Site |
| --- | --- |
| Layout archetype | Extract industry-specific government, public sector & civic services credibility; translate into Public service task flow rather than copying GOV.UK. |
| Header structure | Borrow the decision logic of a direct reference, then rebuild as GOV-style service header. |
| Desktop navigation style | Use clear route grouping for fixed ASH-TRA pages with density tuned to practical. |
| Mobile menu behaviour | Adapt mobile cues into Primary-reference mobile navigation with static-content fallback. |
| Footer structure | Use sitemap completeness and trust routing, rebuilt as service directory footer. |
| Typography mood | Study the mood, then express it through "Arial Narrow" display with "Segoe UI" body and "Lucida Sans" accent labels. |
| Colour palette | Translate reference contrast into the original palette #ffffff, #1d70b8, #ffdd00, #0b0c0c, #ffdd00. |
| Surface system | Use surface hierarchy ideas only; final surfaces are Public service with plain language, blue actions, and high-contrast forms. |
| Hero type | Use the reference to pressure-test the first screen, then build an original Plain service hero. |
| Section rhythm | Extract pacing lessons and apply varied fixed-section composition, not a copied sequence. |
| Card design | Convert card intent into Square service boxes with unique spacing, shape, and state rules. |
| Form pattern | Use friction and reassurance cues to refine the original Public request form. |
| CTA flow | Map intent to Primary-reference CTA hierarchy using Start Request and primary CTA `Start Request`. |
| Asset direction | Translate asset principles into Forms, service lists, notices, public task flows and local ASH-TRA assets. |
| Image treatment | Use crop/contrast lessons without using the reference images, crops, or art direction directly. |
| Icon/illustration style | Extract clarity and hierarchy only; final icon language stays local to CivicAccess. |
| JS interaction ideas | Use as inspiration for Service search, form finder, document filter, alert controls while preserving readable static content. |
| Motion style | Reduce or reshape motion into Minimal state reveal with reduced-motion support. |
| Mobile behaviour | Apply mobile task priority to Primary-reference mobile navigation and responsive section rhythm. |
| What must not be copied | Do not copy code, copy, logos, images, brand assets, exact layouts, exact animations, or proprietary identity. |

## 3. USWDS (direct)

URL: https://designsystem.digital.gov

| Extraction Field | Principle For This ASH-TRA Site |
| --- | --- |
| Layout archetype | Extract industry-specific government, public sector & civic services credibility; translate into Public service task flow rather than copying USWDS. |
| Header structure | Borrow the decision logic of a direct reference, then rebuild as GOV-style service header. |
| Desktop navigation style | Use clear route grouping for fixed ASH-TRA pages with density tuned to practical. |
| Mobile menu behaviour | Adapt mobile cues into Primary-reference mobile navigation with static-content fallback. |
| Footer structure | Use sitemap completeness and trust routing, rebuilt as service directory footer. |
| Typography mood | Study the mood, then express it through "Arial Narrow" display with "Segoe UI" body and "Lucida Sans" accent labels. |
| Colour palette | Translate reference contrast into the original palette #ffffff, #1d70b8, #ffdd00, #0b0c0c, #ffdd00. |
| Surface system | Use surface hierarchy ideas only; final surfaces are Public service with plain language, blue actions, and high-contrast forms. |
| Hero type | Use the reference to pressure-test the first screen, then build an original Plain service hero. |
| Section rhythm | Extract pacing lessons and apply varied fixed-section composition, not a copied sequence. |
| Card design | Convert card intent into Square service boxes with unique spacing, shape, and state rules. |
| Form pattern | Use friction and reassurance cues to refine the original Public request form. |
| CTA flow | Map intent to Primary-reference CTA hierarchy using Start Request and primary CTA `Start Request`. |
| Asset direction | Translate asset principles into Forms, service lists, notices, public task flows and local ASH-TRA assets. |
| Image treatment | Use crop/contrast lessons without using the reference images, crops, or art direction directly. |
| Icon/illustration style | Extract clarity and hierarchy only; final icon language stays local to CivicAccess. |
| JS interaction ideas | Use as inspiration for Service search, form finder, document filter, alert controls while preserving readable static content. |
| Motion style | Reduce or reshape motion into Minimal state reveal with reduced-motion support. |
| Mobile behaviour | Apply mobile task priority to Primary-reference mobile navigation and responsive section rhythm. |
| What must not be copied | Do not copy code, copy, logos, images, brand assets, exact layouts, exact animations, or proprietary identity. |

## 4. NYC.gov (adjacent)

URL: https://www.nyc.gov

| Extraction Field | Principle For This ASH-TRA Site |
| --- | --- |
| Layout archetype | Extract adjacent-category translation for public service access; translate into Public service task flow rather than copying NYC.gov. |
| Header structure | Borrow the decision logic of a adjacent reference, then rebuild as GOV-style service header. |
| Desktop navigation style | Use clear route grouping for fixed ASH-TRA pages with density tuned to practical. |
| Mobile menu behaviour | Adapt mobile cues into Primary-reference mobile navigation with static-content fallback. |
| Footer structure | Use sitemap completeness and trust routing, rebuilt as service directory footer. |
| Typography mood | Study the mood, then express it through "Arial Narrow" display with "Segoe UI" body and "Lucida Sans" accent labels. |
| Colour palette | Translate reference contrast into the original palette #ffffff, #1d70b8, #ffdd00, #0b0c0c, #ffdd00. |
| Surface system | Use surface hierarchy ideas only; final surfaces are Public service with plain language, blue actions, and high-contrast forms. |
| Hero type | Use the reference to pressure-test the first screen, then build an original Plain service hero. |
| Section rhythm | Extract pacing lessons and apply varied fixed-section composition, not a copied sequence. |
| Card design | Convert card intent into Square service boxes with unique spacing, shape, and state rules. |
| Form pattern | Use friction and reassurance cues to refine the original Public request form. |
| CTA flow | Map intent to Primary-reference CTA hierarchy using Start Request and primary CTA `Start Request`. |
| Asset direction | Translate asset principles into Forms, service lists, notices, public task flows and local ASH-TRA assets. |
| Image treatment | Use crop/contrast lessons without using the reference images, crops, or art direction directly. |
| Icon/illustration style | Extract clarity and hierarchy only; final icon language stays local to CivicAccess. |
| JS interaction ideas | Use as inspiration for Service search, form finder, document filter, alert controls while preserving readable static content. |
| Motion style | Reduce or reshape motion into Minimal state reveal with reduced-motion support. |
| Mobile behaviour | Apply mobile task priority to Primary-reference mobile navigation and responsive section rhythm. |
| What must not be copied | Do not copy code, copy, logos, images, brand assets, exact layouts, exact animations, or proprietary identity. |

## 5. Data.gov (adjacent)

URL: https://data.gov

| Extraction Field | Principle For This ASH-TRA Site |
| --- | --- |
| Layout archetype | Extract adjacent-category translation for public service access; translate into Public service task flow rather than copying Data.gov. |
| Header structure | Borrow the decision logic of a adjacent reference, then rebuild as GOV-style service header. |
| Desktop navigation style | Use clear route grouping for fixed ASH-TRA pages with density tuned to practical. |
| Mobile menu behaviour | Adapt mobile cues into Primary-reference mobile navigation with static-content fallback. |
| Footer structure | Use sitemap completeness and trust routing, rebuilt as service directory footer. |
| Typography mood | Study the mood, then express it through "Arial Narrow" display with "Segoe UI" body and "Lucida Sans" accent labels. |
| Colour palette | Translate reference contrast into the original palette #ffffff, #1d70b8, #ffdd00, #0b0c0c, #ffdd00. |
| Surface system | Use surface hierarchy ideas only; final surfaces are Public service with plain language, blue actions, and high-contrast forms. |
| Hero type | Use the reference to pressure-test the first screen, then build an original Plain service hero. |
| Section rhythm | Extract pacing lessons and apply varied fixed-section composition, not a copied sequence. |
| Card design | Convert card intent into Square service boxes with unique spacing, shape, and state rules. |
| Form pattern | Use friction and reassurance cues to refine the original Public request form. |
| CTA flow | Map intent to Primary-reference CTA hierarchy using Start Request and primary CTA `Start Request`. |
| Asset direction | Translate asset principles into Forms, service lists, notices, public task flows and local ASH-TRA assets. |
| Image treatment | Use crop/contrast lessons without using the reference images, crops, or art direction directly. |
| Icon/illustration style | Extract clarity and hierarchy only; final icon language stays local to CivicAccess. |
| JS interaction ideas | Use as inspiration for Service search, form finder, document filter, alert controls while preserving readable static content. |
| Motion style | Reduce or reshape motion into Minimal state reveal with reduced-motion support. |
| Mobile behaviour | Apply mobile task priority to Primary-reference mobile navigation and responsive section rhythm. |
| What must not be copied | Do not copy code, copy, logos, images, brand assets, exact layouts, exact animations, or proprietary identity. |

## 6. Citymapper (contrast)

URL: https://citymapper.com

| Extraction Field | Principle For This ASH-TRA Site |
| --- | --- |
| Layout archetype | Extract contrast-world inspiration to avoid generic civic design; translate into Public service task flow rather than copying Citymapper. |
| Header structure | Borrow the decision logic of a contrast reference, then rebuild as GOV-style service header. |
| Desktop navigation style | Use clear route grouping for fixed ASH-TRA pages with density tuned to practical. |
| Mobile menu behaviour | Adapt mobile cues into Primary-reference mobile navigation with static-content fallback. |
| Footer structure | Use sitemap completeness and trust routing, rebuilt as service directory footer. |
| Typography mood | Study the mood, then express it through "Arial Narrow" display with "Segoe UI" body and "Lucida Sans" accent labels. |
| Colour palette | Translate reference contrast into the original palette #ffffff, #1d70b8, #ffdd00, #0b0c0c, #ffdd00. |
| Surface system | Use surface hierarchy ideas only; final surfaces are Public service with plain language, blue actions, and high-contrast forms. |
| Hero type | Use the reference to pressure-test the first screen, then build an original Plain service hero. |
| Section rhythm | Extract pacing lessons and apply varied fixed-section composition, not a copied sequence. |
| Card design | Convert card intent into Square service boxes with unique spacing, shape, and state rules. |
| Form pattern | Use friction and reassurance cues to refine the original Public request form. |
| CTA flow | Map intent to Primary-reference CTA hierarchy using Start Request and primary CTA `Start Request`. |
| Asset direction | Translate asset principles into Forms, service lists, notices, public task flows and local ASH-TRA assets. |
| Image treatment | Use crop/contrast lessons without using the reference images, crops, or art direction directly. |
| Icon/illustration style | Extract clarity and hierarchy only; final icon language stays local to CivicAccess. |
| JS interaction ideas | Use as inspiration for Service search, form finder, document filter, alert controls while preserving readable static content. |
| Motion style | Reduce or reshape motion into Minimal state reveal with reduced-motion support. |
| Mobile behaviour | Apply mobile task priority to Primary-reference mobile navigation and responsive section rhythm. |
| What must not be copied | Do not copy code, copy, logos, images, brand assets, exact layouts, exact animations, or proprietary identity. |

## 7. Octopus Energy (contrast)

URL: https://octopus.energy

| Extraction Field | Principle For This ASH-TRA Site |
| --- | --- |
| Layout archetype | Extract contrast-world inspiration to avoid generic civic design; translate into Public service task flow rather than copying Octopus Energy. |
| Header structure | Borrow the decision logic of a contrast reference, then rebuild as GOV-style service header. |
| Desktop navigation style | Use clear route grouping for fixed ASH-TRA pages with density tuned to practical. |
| Mobile menu behaviour | Adapt mobile cues into Primary-reference mobile navigation with static-content fallback. |
| Footer structure | Use sitemap completeness and trust routing, rebuilt as service directory footer. |
| Typography mood | Study the mood, then express it through "Arial Narrow" display with "Segoe UI" body and "Lucida Sans" accent labels. |
| Colour palette | Translate reference contrast into the original palette #ffffff, #1d70b8, #ffdd00, #0b0c0c, #ffdd00. |
| Surface system | Use surface hierarchy ideas only; final surfaces are Public service with plain language, blue actions, and high-contrast forms. |
| Hero type | Use the reference to pressure-test the first screen, then build an original Plain service hero. |
| Section rhythm | Extract pacing lessons and apply varied fixed-section composition, not a copied sequence. |
| Card design | Convert card intent into Square service boxes with unique spacing, shape, and state rules. |
| Form pattern | Use friction and reassurance cues to refine the original Public request form. |
| CTA flow | Map intent to Primary-reference CTA hierarchy using Start Request and primary CTA `Start Request`. |
| Asset direction | Translate asset principles into Forms, service lists, notices, public task flows and local ASH-TRA assets. |
| Image treatment | Use crop/contrast lessons without using the reference images, crops, or art direction directly. |
| Icon/illustration style | Extract clarity and hierarchy only; final icon language stays local to CivicAccess. |
| JS interaction ideas | Use as inspiration for Service search, form finder, document filter, alert controls while preserving readable static content. |
| Motion style | Reduce or reshape motion into Minimal state reveal with reduced-motion support. |
| Mobile behaviour | Apply mobile task priority to Primary-reference mobile navigation and responsive section rhythm. |
| What must not be copied | Do not copy code, copy, logos, images, brand assets, exact layouts, exact animations, or proprietary identity. |

## 8. USWDS service form patterns (interaction)

URL: https://designsystem.digital.gov

| Extraction Field | Principle For This ASH-TRA Site |
| --- | --- |
| Layout archetype | Extract interaction reference for Service search, form finder, document filter, alert controls; translate into Public service task flow rather than copying USWDS service form patterns. |
| Header structure | Borrow the decision logic of a interaction reference, then rebuild as GOV-style service header. |
| Desktop navigation style | Use clear route grouping for fixed ASH-TRA pages with density tuned to practical. |
| Mobile menu behaviour | Adapt mobile cues into Primary-reference mobile navigation with static-content fallback. |
| Footer structure | Use sitemap completeness and trust routing, rebuilt as service directory footer. |
| Typography mood | Study the mood, then express it through "Arial Narrow" display with "Segoe UI" body and "Lucida Sans" accent labels. |
| Colour palette | Translate reference contrast into the original palette #ffffff, #1d70b8, #ffdd00, #0b0c0c, #ffdd00. |
| Surface system | Use surface hierarchy ideas only; final surfaces are Public service with plain language, blue actions, and high-contrast forms. |
| Hero type | Use the reference to pressure-test the first screen, then build an original Plain service hero. |
| Section rhythm | Extract pacing lessons and apply varied fixed-section composition, not a copied sequence. |
| Card design | Convert card intent into Square service boxes with unique spacing, shape, and state rules. |
| Form pattern | Use friction and reassurance cues to refine the original Public request form. |
| CTA flow | Map intent to Primary-reference CTA hierarchy using Start Request and primary CTA `Start Request`. |
| Asset direction | Translate asset principles into Forms, service lists, notices, public task flows and local ASH-TRA assets. |
| Image treatment | Use crop/contrast lessons without using the reference images, crops, or art direction directly. |
| Icon/illustration style | Extract clarity and hierarchy only; final icon language stays local to CivicAccess. |
| JS interaction ideas | Use as inspiration for Service search, form finder, document filter, alert controls while preserving readable static content. |
| Motion style | Reduce or reshape motion into Minimal state reveal with reduced-motion support. |
| Mobile behaviour | Apply mobile task priority to Primary-reference mobile navigation and responsive section rhythm. |
| What must not be copied | Do not copy code, copy, logos, images, brand assets, exact layouts, exact animations, or proprietary identity. |

## Originality Commitment

Inspired by these patterns, this ASH-TRA site will use an original design system with different branding, copy, layout, CSS, assets, and interactions.

## Static Authorship Pass

This site folder is now treated as the editable static source for `CivicAccess`. The duplicate secondary header route strip was removed from the static HTML, and the footer markup/CSS was rewritten directly in the site files using the `civic` footer pattern for the `GOV.UK Design System` inspiration direction. Future edits should happen in this site folder rather than regenerating shared templates.

## Portfolio Section Component Pass - 2026-05-11

CivicAccess now treats every main-body section as a static, WordPress-ready component with a component ID, section type, layout variation, pattern family, mobile stacking rule, and editable field map. The pass keeps the GOV.UK Design System inspiration as a principle reference only while using original local markup, CSS, JS, and assets. Repeated section names are deliberately varied so Hero, Services, Process, Results, FAQ, Contact, and CTA patterns do not collapse into one shared layout.


## Homepage Atlas Pass - 2026-05-12

- Homepage content now remains hero-first and CTA-final; the notes/disclaimer block is placed before the final CTA so the page closes on `Start Request`.
- The hero secondary CTA scrolls to `#site-atlas`, and the atlas links every core page plus support, legal, sitemap, recovery, and asset-system routes.
- Homepage section text links now route deeper into the site instead of looping back to Home.
- Atlas variant: `command` for `Forum`, shaped by `Public service task flow`, `Square service boxes`, `Service search, form finder, document filter, alert controls`, and `civic` mobile behaviour.
- No new external inspiration was added; the existing reference pack remains the source for layout, header, navigation, footer, typography, colour, cards, forms, assets, motion, mobile behaviour, and non-copy rules.


## Portfolio Visual QA Pass - 2026-05-12

- Section copy was tightened so reusable blocks explain `Forum` with clearer role, proof, fit, and next-step language instead of repeated placeholder patterns.
- Generic card labels were replaced with section-purpose labels across services, trust, results, editorial, FAQ, and CTA blocks.
- CSS now keeps screenshot and no-JS states visible, disables problematic `content-visibility` reservations for portfolio review, prevents horizontal overflow, and avoids awkward mid-word breaks.
- The pass preserves each site's existing inspiration pack, local assets, component families, section rhythm, and cross-site identity while improving presentation quality across all pages.
