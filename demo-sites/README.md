# ASH-TRA Demo Sites

Fifty independent premium static website folders. Treat each numbered folder as the editable source for that site: HTML, CSS, JavaScript, partials, assets, and docs should be changed in place rather than regenerated from a shared template. Run each site's `scripts/maintain.py all` before publishing, then `scripts/publish.py` after the matching numeric GitHub repo exists.
