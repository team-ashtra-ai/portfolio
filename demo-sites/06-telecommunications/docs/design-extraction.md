# Design Extraction

For every reference, extract principles rather than copying visible identity. The fixed ASH-TRA page and section matrix remains unchanged.

## 1. Starlink (direct)

URL: https://www.starlink.com/

| Extraction Field | Principle For This ASH-TRA Site |
| --- | --- |
| Layout archetype | Extract industry-specific telecommunications, internet & connectivity credibility; translate into Space coverage landing rather than copying Starlink. |
| Header structure | Borrow the decision logic of a direct reference, then rebuild as Uppercase utility header. |
| Desktop navigation style | Use clear route grouping for fixed ASH-TRA pages with density tuned to practical. |
| Mobile menu behaviour | Adapt mobile cues into Primary-reference mobile navigation with static-content fallback. |
| Footer structure | Use sitemap completeness and trust routing, rebuilt as coverage support footer. |
| Typography mood | Study the mood, then express it through Inter display with Aptos body and "Trebuchet MS" accent labels. |
| Colour palette | Translate reference contrast into the original palette #050505, #ffffff, #8b949e, #f5f5f5, #b8c7d9. |
| Surface system | Use surface hierarchy ideas only; final surfaces are Black space utility with full-bleed contrast, uppercase navigation, and availability CTAs. |
| Hero type | Use the reference to pressure-test the first screen, then build an original Black full-bleed coverage hero. |
| Section rhythm | Extract pacing lessons and apply varied fixed-section composition, not a copied sequence. |
| Card design | Convert card intent into Satellite coverage cards with unique spacing, shape, and state rules. |
| Form pattern | Use friction and reassurance cues to refine the original Availability checker form. |
| CTA flow | Map intent to Primary-reference CTA hierarchy using Check Coverage and primary CTA `Check Coverage`. |
| Asset direction | Translate asset principles into Satellite fields, night skies, coverage maps, hardware silhouettes and local ASH-TRA assets. |
| Image treatment | Use crop/contrast lessons without using the reference images, crops, or art direction directly. |
| Icon/illustration style | Extract clarity and hierarchy only; final icon language stays local to SignalNorth. |
| JS interaction ideas | Use as inspiration for Coverage checker mockup, plan filter, outage alert panel while preserving readable static content. |
| Motion style | Reduce or reshape motion into Slow parallax and signal reveal with reduced-motion support. |
| Mobile behaviour | Apply mobile task priority to Primary-reference mobile navigation and responsive section rhythm. |
| What must not be copied | Do not copy code, copy, logos, images, brand assets, exact layouts, exact animations, or proprietary identity. |

## 2. Verizon Business (direct)

URL: https://www.verizon.com/business

| Extraction Field | Principle For This ASH-TRA Site |
| --- | --- |
| Layout archetype | Extract industry-specific telecommunications, internet & connectivity credibility; translate into Space coverage landing rather than copying Verizon Business. |
| Header structure | Borrow the decision logic of a direct reference, then rebuild as Uppercase utility header. |
| Desktop navigation style | Use clear route grouping for fixed ASH-TRA pages with density tuned to practical. |
| Mobile menu behaviour | Adapt mobile cues into Primary-reference mobile navigation with static-content fallback. |
| Footer structure | Use sitemap completeness and trust routing, rebuilt as coverage support footer. |
| Typography mood | Study the mood, then express it through Inter display with Aptos body and "Trebuchet MS" accent labels. |
| Colour palette | Translate reference contrast into the original palette #050505, #ffffff, #8b949e, #f5f5f5, #b8c7d9. |
| Surface system | Use surface hierarchy ideas only; final surfaces are Black space utility with full-bleed contrast, uppercase navigation, and availability CTAs. |
| Hero type | Use the reference to pressure-test the first screen, then build an original Black full-bleed coverage hero. |
| Section rhythm | Extract pacing lessons and apply varied fixed-section composition, not a copied sequence. |
| Card design | Convert card intent into Satellite coverage cards with unique spacing, shape, and state rules. |
| Form pattern | Use friction and reassurance cues to refine the original Availability checker form. |
| CTA flow | Map intent to Primary-reference CTA hierarchy using Check Coverage and primary CTA `Check Coverage`. |
| Asset direction | Translate asset principles into Satellite fields, night skies, coverage maps, hardware silhouettes and local ASH-TRA assets. |
| Image treatment | Use crop/contrast lessons without using the reference images, crops, or art direction directly. |
| Icon/illustration style | Extract clarity and hierarchy only; final icon language stays local to SignalNorth. |
| JS interaction ideas | Use as inspiration for Coverage checker mockup, plan filter, outage alert panel while preserving readable static content. |
| Motion style | Reduce or reshape motion into Slow parallax and signal reveal with reduced-motion support. |
| Mobile behaviour | Apply mobile task priority to Primary-reference mobile navigation and responsive section rhythm. |
| What must not be copied | Do not copy code, copy, logos, images, brand assets, exact layouts, exact animations, or proprietary identity. |

## 3. Google Fiber (direct)

URL: https://fiber.google.com

| Extraction Field | Principle For This ASH-TRA Site |
| --- | --- |
| Layout archetype | Extract industry-specific telecommunications, internet & connectivity credibility; translate into Space coverage landing rather than copying Google Fiber. |
| Header structure | Borrow the decision logic of a direct reference, then rebuild as Uppercase utility header. |
| Desktop navigation style | Use clear route grouping for fixed ASH-TRA pages with density tuned to practical. |
| Mobile menu behaviour | Adapt mobile cues into Primary-reference mobile navigation with static-content fallback. |
| Footer structure | Use sitemap completeness and trust routing, rebuilt as coverage support footer. |
| Typography mood | Study the mood, then express it through Inter display with Aptos body and "Trebuchet MS" accent labels. |
| Colour palette | Translate reference contrast into the original palette #050505, #ffffff, #8b949e, #f5f5f5, #b8c7d9. |
| Surface system | Use surface hierarchy ideas only; final surfaces are Black space utility with full-bleed contrast, uppercase navigation, and availability CTAs. |
| Hero type | Use the reference to pressure-test the first screen, then build an original Black full-bleed coverage hero. |
| Section rhythm | Extract pacing lessons and apply varied fixed-section composition, not a copied sequence. |
| Card design | Convert card intent into Satellite coverage cards with unique spacing, shape, and state rules. |
| Form pattern | Use friction and reassurance cues to refine the original Availability checker form. |
| CTA flow | Map intent to Primary-reference CTA hierarchy using Check Coverage and primary CTA `Check Coverage`. |
| Asset direction | Translate asset principles into Satellite fields, night skies, coverage maps, hardware silhouettes and local ASH-TRA assets. |
| Image treatment | Use crop/contrast lessons without using the reference images, crops, or art direction directly. |
| Icon/illustration style | Extract clarity and hierarchy only; final icon language stays local to SignalNorth. |
| JS interaction ideas | Use as inspiration for Coverage checker mockup, plan filter, outage alert panel while preserving readable static content. |
| Motion style | Reduce or reshape motion into Slow parallax and signal reveal with reduced-motion support. |
| Mobile behaviour | Apply mobile task priority to Primary-reference mobile navigation and responsive section rhythm. |
| What must not be copied | Do not copy code, copy, logos, images, brand assets, exact layouts, exact animations, or proprietary identity. |

## 4. Ting Internet (adjacent)

URL: https://ting.com/internet

| Extraction Field | Principle For This ASH-TRA Site |
| --- | --- |
| Layout archetype | Extract adjacent-category translation for availability and reliability; translate into Space coverage landing rather than copying Ting Internet. |
| Header structure | Borrow the decision logic of a adjacent reference, then rebuild as Uppercase utility header. |
| Desktop navigation style | Use clear route grouping for fixed ASH-TRA pages with density tuned to practical. |
| Mobile menu behaviour | Adapt mobile cues into Primary-reference mobile navigation with static-content fallback. |
| Footer structure | Use sitemap completeness and trust routing, rebuilt as coverage support footer. |
| Typography mood | Study the mood, then express it through Inter display with Aptos body and "Trebuchet MS" accent labels. |
| Colour palette | Translate reference contrast into the original palette #050505, #ffffff, #8b949e, #f5f5f5, #b8c7d9. |
| Surface system | Use surface hierarchy ideas only; final surfaces are Black space utility with full-bleed contrast, uppercase navigation, and availability CTAs. |
| Hero type | Use the reference to pressure-test the first screen, then build an original Black full-bleed coverage hero. |
| Section rhythm | Extract pacing lessons and apply varied fixed-section composition, not a copied sequence. |
| Card design | Convert card intent into Satellite coverage cards with unique spacing, shape, and state rules. |
| Form pattern | Use friction and reassurance cues to refine the original Availability checker form. |
| CTA flow | Map intent to Primary-reference CTA hierarchy using Check Coverage and primary CTA `Check Coverage`. |
| Asset direction | Translate asset principles into Satellite fields, night skies, coverage maps, hardware silhouettes and local ASH-TRA assets. |
| Image treatment | Use crop/contrast lessons without using the reference images, crops, or art direction directly. |
| Icon/illustration style | Extract clarity and hierarchy only; final icon language stays local to SignalNorth. |
| JS interaction ideas | Use as inspiration for Coverage checker mockup, plan filter, outage alert panel while preserving readable static content. |
| Motion style | Reduce or reshape motion into Slow parallax and signal reveal with reduced-motion support. |
| Mobile behaviour | Apply mobile task priority to Primary-reference mobile navigation and responsive section rhythm. |
| What must not be copied | Do not copy code, copy, logos, images, brand assets, exact layouts, exact animations, or proprietary identity. |

## 5. T-Mobile Business (adjacent)

URL: https://www.t-mobile.com/business

| Extraction Field | Principle For This ASH-TRA Site |
| --- | --- |
| Layout archetype | Extract adjacent-category translation for availability and reliability; translate into Space coverage landing rather than copying T-Mobile Business. |
| Header structure | Borrow the decision logic of a adjacent reference, then rebuild as Uppercase utility header. |
| Desktop navigation style | Use clear route grouping for fixed ASH-TRA pages with density tuned to practical. |
| Mobile menu behaviour | Adapt mobile cues into Primary-reference mobile navigation with static-content fallback. |
| Footer structure | Use sitemap completeness and trust routing, rebuilt as coverage support footer. |
| Typography mood | Study the mood, then express it through Inter display with Aptos body and "Trebuchet MS" accent labels. |
| Colour palette | Translate reference contrast into the original palette #050505, #ffffff, #8b949e, #f5f5f5, #b8c7d9. |
| Surface system | Use surface hierarchy ideas only; final surfaces are Black space utility with full-bleed contrast, uppercase navigation, and availability CTAs. |
| Hero type | Use the reference to pressure-test the first screen, then build an original Black full-bleed coverage hero. |
| Section rhythm | Extract pacing lessons and apply varied fixed-section composition, not a copied sequence. |
| Card design | Convert card intent into Satellite coverage cards with unique spacing, shape, and state rules. |
| Form pattern | Use friction and reassurance cues to refine the original Availability checker form. |
| CTA flow | Map intent to Primary-reference CTA hierarchy using Check Coverage and primary CTA `Check Coverage`. |
| Asset direction | Translate asset principles into Satellite fields, night skies, coverage maps, hardware silhouettes and local ASH-TRA assets. |
| Image treatment | Use crop/contrast lessons without using the reference images, crops, or art direction directly. |
| Icon/illustration style | Extract clarity and hierarchy only; final icon language stays local to SignalNorth. |
| JS interaction ideas | Use as inspiration for Coverage checker mockup, plan filter, outage alert panel while preserving readable static content. |
| Motion style | Reduce or reshape motion into Slow parallax and signal reveal with reduced-motion support. |
| Mobile behaviour | Apply mobile task priority to Primary-reference mobile navigation and responsive section rhythm. |
| What must not be copied | Do not copy code, copy, logos, images, brand assets, exact layouts, exact animations, or proprietary identity. |

## 6. Citymapper (contrast)

URL: https://citymapper.com

| Extraction Field | Principle For This ASH-TRA Site |
| --- | --- |
| Layout archetype | Extract contrast-world inspiration to avoid generic professional design; translate into Space coverage landing rather than copying Citymapper. |
| Header structure | Borrow the decision logic of a contrast reference, then rebuild as Uppercase utility header. |
| Desktop navigation style | Use clear route grouping for fixed ASH-TRA pages with density tuned to practical. |
| Mobile menu behaviour | Adapt mobile cues into Primary-reference mobile navigation with static-content fallback. |
| Footer structure | Use sitemap completeness and trust routing, rebuilt as coverage support footer. |
| Typography mood | Study the mood, then express it through Inter display with Aptos body and "Trebuchet MS" accent labels. |
| Colour palette | Translate reference contrast into the original palette #050505, #ffffff, #8b949e, #f5f5f5, #b8c7d9. |
| Surface system | Use surface hierarchy ideas only; final surfaces are Black space utility with full-bleed contrast, uppercase navigation, and availability CTAs. |
| Hero type | Use the reference to pressure-test the first screen, then build an original Black full-bleed coverage hero. |
| Section rhythm | Extract pacing lessons and apply varied fixed-section composition, not a copied sequence. |
| Card design | Convert card intent into Satellite coverage cards with unique spacing, shape, and state rules. |
| Form pattern | Use friction and reassurance cues to refine the original Availability checker form. |
| CTA flow | Map intent to Primary-reference CTA hierarchy using Check Coverage and primary CTA `Check Coverage`. |
| Asset direction | Translate asset principles into Satellite fields, night skies, coverage maps, hardware silhouettes and local ASH-TRA assets. |
| Image treatment | Use crop/contrast lessons without using the reference images, crops, or art direction directly. |
| Icon/illustration style | Extract clarity and hierarchy only; final icon language stays local to SignalNorth. |
| JS interaction ideas | Use as inspiration for Coverage checker mockup, plan filter, outage alert panel while preserving readable static content. |
| Motion style | Reduce or reshape motion into Slow parallax and signal reveal with reduced-motion support. |
| Mobile behaviour | Apply mobile task priority to Primary-reference mobile navigation and responsive section rhythm. |
| What must not be copied | Do not copy code, copy, logos, images, brand assets, exact layouts, exact animations, or proprietary identity. |

## 7. Octopus Energy (contrast)

URL: https://octopus.energy

| Extraction Field | Principle For This ASH-TRA Site |
| --- | --- |
| Layout archetype | Extract contrast-world inspiration to avoid generic professional design; translate into Space coverage landing rather than copying Octopus Energy. |
| Header structure | Borrow the decision logic of a contrast reference, then rebuild as Uppercase utility header. |
| Desktop navigation style | Use clear route grouping for fixed ASH-TRA pages with density tuned to practical. |
| Mobile menu behaviour | Adapt mobile cues into Primary-reference mobile navigation with static-content fallback. |
| Footer structure | Use sitemap completeness and trust routing, rebuilt as coverage support footer. |
| Typography mood | Study the mood, then express it through Inter display with Aptos body and "Trebuchet MS" accent labels. |
| Colour palette | Translate reference contrast into the original palette #050505, #ffffff, #8b949e, #f5f5f5, #b8c7d9. |
| Surface system | Use surface hierarchy ideas only; final surfaces are Black space utility with full-bleed contrast, uppercase navigation, and availability CTAs. |
| Hero type | Use the reference to pressure-test the first screen, then build an original Black full-bleed coverage hero. |
| Section rhythm | Extract pacing lessons and apply varied fixed-section composition, not a copied sequence. |
| Card design | Convert card intent into Satellite coverage cards with unique spacing, shape, and state rules. |
| Form pattern | Use friction and reassurance cues to refine the original Availability checker form. |
| CTA flow | Map intent to Primary-reference CTA hierarchy using Check Coverage and primary CTA `Check Coverage`. |
| Asset direction | Translate asset principles into Satellite fields, night skies, coverage maps, hardware silhouettes and local ASH-TRA assets. |
| Image treatment | Use crop/contrast lessons without using the reference images, crops, or art direction directly. |
| Icon/illustration style | Extract clarity and hierarchy only; final icon language stays local to SignalNorth. |
| JS interaction ideas | Use as inspiration for Coverage checker mockup, plan filter, outage alert panel while preserving readable static content. |
| Motion style | Reduce or reshape motion into Slow parallax and signal reveal with reduced-motion support. |
| Mobile behaviour | Apply mobile task priority to Primary-reference mobile navigation and responsive section rhythm. |
| What must not be copied | Do not copy code, copy, logos, images, brand assets, exact layouts, exact animations, or proprietary identity. |

## 8. Mobbin availability checker patterns (interaction)

URL: https://mobbin.com

| Extraction Field | Principle For This ASH-TRA Site |
| --- | --- |
| Layout archetype | Extract interaction reference for Coverage checker mockup, plan filter, outage alert panel; translate into Space coverage landing rather than copying Mobbin availability checker patterns. |
| Header structure | Borrow the decision logic of a interaction reference, then rebuild as Uppercase utility header. |
| Desktop navigation style | Use clear route grouping for fixed ASH-TRA pages with density tuned to practical. |
| Mobile menu behaviour | Adapt mobile cues into Primary-reference mobile navigation with static-content fallback. |
| Footer structure | Use sitemap completeness and trust routing, rebuilt as coverage support footer. |
| Typography mood | Study the mood, then express it through Inter display with Aptos body and "Trebuchet MS" accent labels. |
| Colour palette | Translate reference contrast into the original palette #050505, #ffffff, #8b949e, #f5f5f5, #b8c7d9. |
| Surface system | Use surface hierarchy ideas only; final surfaces are Black space utility with full-bleed contrast, uppercase navigation, and availability CTAs. |
| Hero type | Use the reference to pressure-test the first screen, then build an original Black full-bleed coverage hero. |
| Section rhythm | Extract pacing lessons and apply varied fixed-section composition, not a copied sequence. |
| Card design | Convert card intent into Satellite coverage cards with unique spacing, shape, and state rules. |
| Form pattern | Use friction and reassurance cues to refine the original Availability checker form. |
| CTA flow | Map intent to Primary-reference CTA hierarchy using Check Coverage and primary CTA `Check Coverage`. |
| Asset direction | Translate asset principles into Satellite fields, night skies, coverage maps, hardware silhouettes and local ASH-TRA assets. |
| Image treatment | Use crop/contrast lessons without using the reference images, crops, or art direction directly. |
| Icon/illustration style | Extract clarity and hierarchy only; final icon language stays local to SignalNorth. |
| JS interaction ideas | Use as inspiration for Coverage checker mockup, plan filter, outage alert panel while preserving readable static content. |
| Motion style | Reduce or reshape motion into Slow parallax and signal reveal with reduced-motion support. |
| Mobile behaviour | Apply mobile task priority to Primary-reference mobile navigation and responsive section rhythm. |
| What must not be copied | Do not copy code, copy, logos, images, brand assets, exact layouts, exact animations, or proprietary identity. |

## Originality Commitment

Inspired by these patterns, this ASH-TRA site will use an original design system with different branding, copy, layout, CSS, assets, and interactions.

## Static Authorship Pass

This site folder is now treated as the editable static source for `SignalNorth`. The duplicate secondary header route strip was removed from the static HTML, and the footer markup/CSS was rewritten directly in the site files using the `immersive` footer pattern for the `Starlink` inspiration direction. Future edits should happen in this site folder rather than regenerating shared templates.

## Portfolio Section Component Pass - 2026-05-11

SignalNorth now treats every main-body section as a static, WordPress-ready component with a component ID, section type, layout variation, pattern family, mobile stacking rule, and editable field map. The pass keeps the Starlink inspiration as a principle reference only while using original local markup, CSS, JS, and assets. Repeated section names are deliberately varied so Hero, Services, Process, Results, FAQ, Contact, and CTA patterns do not collapse into one shared layout.


## Homepage Atlas Pass - 2026-05-12

- Homepage content now remains hero-first and CTA-final; the notes/disclaimer block is placed before the final CTA so the page closes on `Check Coverage`.
- The hero secondary CTA scrolls to `#site-atlas`, and the atlas links every core page plus support, legal, sitemap, recovery, and asset-system routes.
- Homepage section text links now route deeper into the site instead of looping back to Home.
- Atlas variant: `command` for `Signal`, shaped by `Space coverage landing`, `Satellite coverage cards`, `Coverage checker mockup, plan filter, outage alert panel`, and `professional` mobile behaviour.
- No new external inspiration was added; the existing reference pack remains the source for layout, header, navigation, footer, typography, colour, cards, forms, assets, motion, mobile behaviour, and non-copy rules.


## Portfolio Visual QA Pass - 2026-05-12

- Section copy was tightened so reusable blocks explain `Signal` with clearer role, proof, fit, and next-step language instead of repeated placeholder patterns.
- Generic card labels were replaced with section-purpose labels across services, trust, results, editorial, FAQ, and CTA blocks.
- CSS now keeps screenshot and no-JS states visible, disables problematic `content-visibility` reservations for portfolio review, prevents horizontal overflow, and avoids awkward mid-word breaks.
- The pass preserves each site's existing inspiration pack, local assets, component families, section rhythm, and cross-site identity while improving presentation quality across all pages.
