# Design Extraction

For every reference, extract principles rather than copying visible identity. The fixed ASH-TRA page and section matrix remains unchanged.

## 1. Apple Store (direct)

URL: https://www.apple.com/store

| Extraction Field | Principle For This ASH-TRA Site |
| --- | --- |
| Layout archetype | Extract industry-specific retail, shops & consumer sales credibility; translate into Minimal product store rather than copying Apple Store. |
| Header structure | Borrow the decision logic of a direct reference, then rebuild as Clean shop header. |
| Desktop navigation style | Use clear route grouping for fixed ASH-TRA pages with density tuned to medium. |
| Mobile menu behaviour | Adapt mobile cues into Primary-reference mobile navigation with static-content fallback. |
| Footer structure | Use sitemap completeness and trust routing, rebuilt as store finder footer. |
| Typography mood | Study the mood, then express it through Aptos display with "Trebuchet MS" body and "Palatino Linotype" accent labels. |
| Colour palette | Translate reference contrast into the original palette #f5f5f7, #1d1d1f, #0071e3, #1d1d1f, #a1a1a6. |
| Surface system | Use surface hierarchy ideas only; final surfaces are Retail minimalism with soft grey surfaces, product tiles, and clear shopping paths. |
| Hero type | Use the reference to pressure-test the first screen, then build an original Soft product shelf hero. |
| Section rhythm | Extract pacing lessons and apply varied fixed-section composition, not a copied sequence. |
| Card design | Convert card intent into Rounded product tiles with unique spacing, shape, and state rules. |
| Form pattern | Use friction and reassurance cues to refine the original Store finder form. |
| CTA flow | Map intent to Primary-reference CTA hierarchy using Visit Shop and primary CTA `Visit Shop`. |
| Asset direction | Translate asset principles into Product tiles, store spaces, service cards, device-like panels and local ASH-TRA assets. |
| Image treatment | Use crop/contrast lessons without using the reference images, crops, or art direction directly. |
| Icon/illustration style | Extract clarity and hierarchy only; final icon language stays local to CornerGoods. |
| JS interaction ideas | Use as inspiration for Product/category filter, loyalty signup, store finder while preserving readable static content. |
| Motion style | Reduce or reshape motion into Polished product reveal with reduced-motion support. |
| Mobile behaviour | Apply mobile task priority to Primary-reference mobile navigation and responsive section rhythm. |
| What must not be copied | Do not copy code, copy, logos, images, brand assets, exact layouts, exact animations, or proprietary identity. |

## 2. Nike (direct)

URL: https://www.nike.com

| Extraction Field | Principle For This ASH-TRA Site |
| --- | --- |
| Layout archetype | Extract industry-specific retail, shops & consumer sales credibility; translate into Minimal product store rather than copying Nike. |
| Header structure | Borrow the decision logic of a direct reference, then rebuild as Clean shop header. |
| Desktop navigation style | Use clear route grouping for fixed ASH-TRA pages with density tuned to medium. |
| Mobile menu behaviour | Adapt mobile cues into Primary-reference mobile navigation with static-content fallback. |
| Footer structure | Use sitemap completeness and trust routing, rebuilt as store finder footer. |
| Typography mood | Study the mood, then express it through Aptos display with "Trebuchet MS" body and "Palatino Linotype" accent labels. |
| Colour palette | Translate reference contrast into the original palette #f5f5f7, #1d1d1f, #0071e3, #1d1d1f, #a1a1a6. |
| Surface system | Use surface hierarchy ideas only; final surfaces are Retail minimalism with soft grey surfaces, product tiles, and clear shopping paths. |
| Hero type | Use the reference to pressure-test the first screen, then build an original Soft product shelf hero. |
| Section rhythm | Extract pacing lessons and apply varied fixed-section composition, not a copied sequence. |
| Card design | Convert card intent into Rounded product tiles with unique spacing, shape, and state rules. |
| Form pattern | Use friction and reassurance cues to refine the original Store finder form. |
| CTA flow | Map intent to Primary-reference CTA hierarchy using Visit Shop and primary CTA `Visit Shop`. |
| Asset direction | Translate asset principles into Product tiles, store spaces, service cards, device-like panels and local ASH-TRA assets. |
| Image treatment | Use crop/contrast lessons without using the reference images, crops, or art direction directly. |
| Icon/illustration style | Extract clarity and hierarchy only; final icon language stays local to CornerGoods. |
| JS interaction ideas | Use as inspiration for Product/category filter, loyalty signup, store finder while preserving readable static content. |
| Motion style | Reduce or reshape motion into Polished product reveal with reduced-motion support. |
| Mobile behaviour | Apply mobile task priority to Primary-reference mobile navigation and responsive section rhythm. |
| What must not be copied | Do not copy code, copy, logos, images, brand assets, exact layouts, exact animations, or proprietary identity. |

## 3. Patagonia (direct)

URL: https://www.patagonia.com

| Extraction Field | Principle For This ASH-TRA Site |
| --- | --- |
| Layout archetype | Extract industry-specific retail, shops & consumer sales credibility; translate into Minimal product store rather than copying Patagonia. |
| Header structure | Borrow the decision logic of a direct reference, then rebuild as Clean shop header. |
| Desktop navigation style | Use clear route grouping for fixed ASH-TRA pages with density tuned to medium. |
| Mobile menu behaviour | Adapt mobile cues into Primary-reference mobile navigation with static-content fallback. |
| Footer structure | Use sitemap completeness and trust routing, rebuilt as store finder footer. |
| Typography mood | Study the mood, then express it through Aptos display with "Trebuchet MS" body and "Palatino Linotype" accent labels. |
| Colour palette | Translate reference contrast into the original palette #f5f5f7, #1d1d1f, #0071e3, #1d1d1f, #a1a1a6. |
| Surface system | Use surface hierarchy ideas only; final surfaces are Retail minimalism with soft grey surfaces, product tiles, and clear shopping paths. |
| Hero type | Use the reference to pressure-test the first screen, then build an original Soft product shelf hero. |
| Section rhythm | Extract pacing lessons and apply varied fixed-section composition, not a copied sequence. |
| Card design | Convert card intent into Rounded product tiles with unique spacing, shape, and state rules. |
| Form pattern | Use friction and reassurance cues to refine the original Store finder form. |
| CTA flow | Map intent to Primary-reference CTA hierarchy using Visit Shop and primary CTA `Visit Shop`. |
| Asset direction | Translate asset principles into Product tiles, store spaces, service cards, device-like panels and local ASH-TRA assets. |
| Image treatment | Use crop/contrast lessons without using the reference images, crops, or art direction directly. |
| Icon/illustration style | Extract clarity and hierarchy only; final icon language stays local to CornerGoods. |
| JS interaction ideas | Use as inspiration for Product/category filter, loyalty signup, store finder while preserving readable static content. |
| Motion style | Reduce or reshape motion into Polished product reveal with reduced-motion support. |
| Mobile behaviour | Apply mobile task priority to Primary-reference mobile navigation and responsive section rhythm. |
| What must not be copied | Do not copy code, copy, logos, images, brand assets, exact layouts, exact animations, or proprietary identity. |

## 4. Muji (adjacent)

URL: https://www.muji.com

| Extraction Field | Principle For This ASH-TRA Site |
| --- | --- |
| Layout archetype | Extract adjacent-category translation for convenience and offer discovery; translate into Minimal product store rather than copying Muji. |
| Header structure | Borrow the decision logic of a adjacent reference, then rebuild as Clean shop header. |
| Desktop navigation style | Use clear route grouping for fixed ASH-TRA pages with density tuned to medium. |
| Mobile menu behaviour | Adapt mobile cues into Primary-reference mobile navigation with static-content fallback. |
| Footer structure | Use sitemap completeness and trust routing, rebuilt as store finder footer. |
| Typography mood | Study the mood, then express it through Aptos display with "Trebuchet MS" body and "Palatino Linotype" accent labels. |
| Colour palette | Translate reference contrast into the original palette #f5f5f7, #1d1d1f, #0071e3, #1d1d1f, #a1a1a6. |
| Surface system | Use surface hierarchy ideas only; final surfaces are Retail minimalism with soft grey surfaces, product tiles, and clear shopping paths. |
| Hero type | Use the reference to pressure-test the first screen, then build an original Soft product shelf hero. |
| Section rhythm | Extract pacing lessons and apply varied fixed-section composition, not a copied sequence. |
| Card design | Convert card intent into Rounded product tiles with unique spacing, shape, and state rules. |
| Form pattern | Use friction and reassurance cues to refine the original Store finder form. |
| CTA flow | Map intent to Primary-reference CTA hierarchy using Visit Shop and primary CTA `Visit Shop`. |
| Asset direction | Translate asset principles into Product tiles, store spaces, service cards, device-like panels and local ASH-TRA assets. |
| Image treatment | Use crop/contrast lessons without using the reference images, crops, or art direction directly. |
| Icon/illustration style | Extract clarity and hierarchy only; final icon language stays local to CornerGoods. |
| JS interaction ideas | Use as inspiration for Product/category filter, loyalty signup, store finder while preserving readable static content. |
| Motion style | Reduce or reshape motion into Polished product reveal with reduced-motion support. |
| Mobile behaviour | Apply mobile task priority to Primary-reference mobile navigation and responsive section rhythm. |
| What must not be copied | Do not copy code, copy, logos, images, brand assets, exact layouts, exact animations, or proprietary identity. |

## 5. Target (adjacent)

URL: https://www.target.com

| Extraction Field | Principle For This ASH-TRA Site |
| --- | --- |
| Layout archetype | Extract adjacent-category translation for convenience and offer discovery; translate into Minimal product store rather than copying Target. |
| Header structure | Borrow the decision logic of a adjacent reference, then rebuild as Clean shop header. |
| Desktop navigation style | Use clear route grouping for fixed ASH-TRA pages with density tuned to medium. |
| Mobile menu behaviour | Adapt mobile cues into Primary-reference mobile navigation with static-content fallback. |
| Footer structure | Use sitemap completeness and trust routing, rebuilt as store finder footer. |
| Typography mood | Study the mood, then express it through Aptos display with "Trebuchet MS" body and "Palatino Linotype" accent labels. |
| Colour palette | Translate reference contrast into the original palette #f5f5f7, #1d1d1f, #0071e3, #1d1d1f, #a1a1a6. |
| Surface system | Use surface hierarchy ideas only; final surfaces are Retail minimalism with soft grey surfaces, product tiles, and clear shopping paths. |
| Hero type | Use the reference to pressure-test the first screen, then build an original Soft product shelf hero. |
| Section rhythm | Extract pacing lessons and apply varied fixed-section composition, not a copied sequence. |
| Card design | Convert card intent into Rounded product tiles with unique spacing, shape, and state rules. |
| Form pattern | Use friction and reassurance cues to refine the original Store finder form. |
| CTA flow | Map intent to Primary-reference CTA hierarchy using Visit Shop and primary CTA `Visit Shop`. |
| Asset direction | Translate asset principles into Product tiles, store spaces, service cards, device-like panels and local ASH-TRA assets. |
| Image treatment | Use crop/contrast lessons without using the reference images, crops, or art direction directly. |
| Icon/illustration style | Extract clarity and hierarchy only; final icon language stays local to CornerGoods. |
| JS interaction ideas | Use as inspiration for Product/category filter, loyalty signup, store finder while preserving readable static content. |
| Motion style | Reduce or reshape motion into Polished product reveal with reduced-motion support. |
| Mobile behaviour | Apply mobile task priority to Primary-reference mobile navigation and responsive section rhythm. |
| What must not be copied | Do not copy code, copy, logos, images, brand assets, exact layouts, exact animations, or proprietary identity. |

## 6. Aesop (contrast)

URL: https://www.aesop.com

| Extraction Field | Principle For This ASH-TRA Site |
| --- | --- |
| Layout archetype | Extract contrast-world inspiration to avoid generic commerce design; translate into Minimal product store rather than copying Aesop. |
| Header structure | Borrow the decision logic of a contrast reference, then rebuild as Clean shop header. |
| Desktop navigation style | Use clear route grouping for fixed ASH-TRA pages with density tuned to medium. |
| Mobile menu behaviour | Adapt mobile cues into Primary-reference mobile navigation with static-content fallback. |
| Footer structure | Use sitemap completeness and trust routing, rebuilt as store finder footer. |
| Typography mood | Study the mood, then express it through Aptos display with "Trebuchet MS" body and "Palatino Linotype" accent labels. |
| Colour palette | Translate reference contrast into the original palette #f5f5f7, #1d1d1f, #0071e3, #1d1d1f, #a1a1a6. |
| Surface system | Use surface hierarchy ideas only; final surfaces are Retail minimalism with soft grey surfaces, product tiles, and clear shopping paths. |
| Hero type | Use the reference to pressure-test the first screen, then build an original Soft product shelf hero. |
| Section rhythm | Extract pacing lessons and apply varied fixed-section composition, not a copied sequence. |
| Card design | Convert card intent into Rounded product tiles with unique spacing, shape, and state rules. |
| Form pattern | Use friction and reassurance cues to refine the original Store finder form. |
| CTA flow | Map intent to Primary-reference CTA hierarchy using Visit Shop and primary CTA `Visit Shop`. |
| Asset direction | Translate asset principles into Product tiles, store spaces, service cards, device-like panels and local ASH-TRA assets. |
| Image treatment | Use crop/contrast lessons without using the reference images, crops, or art direction directly. |
| Icon/illustration style | Extract clarity and hierarchy only; final icon language stays local to CornerGoods. |
| JS interaction ideas | Use as inspiration for Product/category filter, loyalty signup, store finder while preserving readable static content. |
| Motion style | Reduce or reshape motion into Polished product reveal with reduced-motion support. |
| Mobile behaviour | Apply mobile task priority to Primary-reference mobile navigation and responsive section rhythm. |
| What must not be copied | Do not copy code, copy, logos, images, brand assets, exact layouts, exact animations, or proprietary identity. |

## 7. Oatly (contrast)

URL: https://www.oatly.com

| Extraction Field | Principle For This ASH-TRA Site |
| --- | --- |
| Layout archetype | Extract contrast-world inspiration to avoid generic commerce design; translate into Minimal product store rather than copying Oatly. |
| Header structure | Borrow the decision logic of a contrast reference, then rebuild as Clean shop header. |
| Desktop navigation style | Use clear route grouping for fixed ASH-TRA pages with density tuned to medium. |
| Mobile menu behaviour | Adapt mobile cues into Primary-reference mobile navigation with static-content fallback. |
| Footer structure | Use sitemap completeness and trust routing, rebuilt as store finder footer. |
| Typography mood | Study the mood, then express it through Aptos display with "Trebuchet MS" body and "Palatino Linotype" accent labels. |
| Colour palette | Translate reference contrast into the original palette #f5f5f7, #1d1d1f, #0071e3, #1d1d1f, #a1a1a6. |
| Surface system | Use surface hierarchy ideas only; final surfaces are Retail minimalism with soft grey surfaces, product tiles, and clear shopping paths. |
| Hero type | Use the reference to pressure-test the first screen, then build an original Soft product shelf hero. |
| Section rhythm | Extract pacing lessons and apply varied fixed-section composition, not a copied sequence. |
| Card design | Convert card intent into Rounded product tiles with unique spacing, shape, and state rules. |
| Form pattern | Use friction and reassurance cues to refine the original Store finder form. |
| CTA flow | Map intent to Primary-reference CTA hierarchy using Visit Shop and primary CTA `Visit Shop`. |
| Asset direction | Translate asset principles into Product tiles, store spaces, service cards, device-like panels and local ASH-TRA assets. |
| Image treatment | Use crop/contrast lessons without using the reference images, crops, or art direction directly. |
| Icon/illustration style | Extract clarity and hierarchy only; final icon language stays local to CornerGoods. |
| JS interaction ideas | Use as inspiration for Product/category filter, loyalty signup, store finder while preserving readable static content. |
| Motion style | Reduce or reshape motion into Polished product reveal with reduced-motion support. |
| Mobile behaviour | Apply mobile task priority to Primary-reference mobile navigation and responsive section rhythm. |
| What must not be copied | Do not copy code, copy, logos, images, brand assets, exact layouts, exact animations, or proprietary identity. |

## 8. Mobbin category and store finder patterns (interaction)

URL: https://mobbin.com

| Extraction Field | Principle For This ASH-TRA Site |
| --- | --- |
| Layout archetype | Extract interaction reference for Product/category filter, loyalty signup, store finder; translate into Minimal product store rather than copying Mobbin category and store finder patterns. |
| Header structure | Borrow the decision logic of a interaction reference, then rebuild as Clean shop header. |
| Desktop navigation style | Use clear route grouping for fixed ASH-TRA pages with density tuned to medium. |
| Mobile menu behaviour | Adapt mobile cues into Primary-reference mobile navigation with static-content fallback. |
| Footer structure | Use sitemap completeness and trust routing, rebuilt as store finder footer. |
| Typography mood | Study the mood, then express it through Aptos display with "Trebuchet MS" body and "Palatino Linotype" accent labels. |
| Colour palette | Translate reference contrast into the original palette #f5f5f7, #1d1d1f, #0071e3, #1d1d1f, #a1a1a6. |
| Surface system | Use surface hierarchy ideas only; final surfaces are Retail minimalism with soft grey surfaces, product tiles, and clear shopping paths. |
| Hero type | Use the reference to pressure-test the first screen, then build an original Soft product shelf hero. |
| Section rhythm | Extract pacing lessons and apply varied fixed-section composition, not a copied sequence. |
| Card design | Convert card intent into Rounded product tiles with unique spacing, shape, and state rules. |
| Form pattern | Use friction and reassurance cues to refine the original Store finder form. |
| CTA flow | Map intent to Primary-reference CTA hierarchy using Visit Shop and primary CTA `Visit Shop`. |
| Asset direction | Translate asset principles into Product tiles, store spaces, service cards, device-like panels and local ASH-TRA assets. |
| Image treatment | Use crop/contrast lessons without using the reference images, crops, or art direction directly. |
| Icon/illustration style | Extract clarity and hierarchy only; final icon language stays local to CornerGoods. |
| JS interaction ideas | Use as inspiration for Product/category filter, loyalty signup, store finder while preserving readable static content. |
| Motion style | Reduce or reshape motion into Polished product reveal with reduced-motion support. |
| Mobile behaviour | Apply mobile task priority to Primary-reference mobile navigation and responsive section rhythm. |
| What must not be copied | Do not copy code, copy, logos, images, brand assets, exact layouts, exact animations, or proprietary identity. |

## Originality Commitment

Inspired by these patterns, this ASH-TRA site will use an original design system with different branding, copy, layout, CSS, assets, and interactions.

## Static Authorship Pass

This site folder is now treated as the editable static source for `CornerGoods`. The duplicate secondary header route strip was removed from the static HTML, and the footer markup/CSS was rewritten directly in the site files using the `product` footer pattern for the `Apple Store` inspiration direction. Future edits should happen in this site folder rather than regenerating shared templates.

## Portfolio Section Component Pass - 2026-05-11

CornerGoods now treats every main-body section as a static, WordPress-ready component with a component ID, section type, layout variation, pattern family, mobile stacking rule, and editable field map. The pass keeps the Apple Store inspiration as a principle reference only while using original local markup, CSS, JS, and assets. Repeated section names are deliberately varied so Hero, Services, Process, Results, FAQ, Contact, and CTA patterns do not collapse into one shared layout.


## Homepage Atlas Pass - 2026-05-12

- Homepage content now remains hero-first and CTA-final; the notes/disclaimer block is placed before the final CTA so the page closes on `Visit Shop`.
- The hero secondary CTA scrolls to `#site-atlas`, and the atlas links every core page plus support, legal, sitemap, recovery, and asset-system routes.
- Homepage section text links now route deeper into the site instead of looping back to Home.
- Atlas variant: `magazine` for `Corner`, shaped by `Minimal product store`, `Rounded product tiles`, `Product/category filter, loyalty signup, store finder`, and `commerce` mobile behaviour.
- No new external inspiration was added; the existing reference pack remains the source for layout, header, navigation, footer, typography, colour, cards, forms, assets, motion, mobile behaviour, and non-copy rules.


## Portfolio Visual QA Pass - 2026-05-12

- Section copy was tightened so reusable blocks explain `Corner` with clearer role, proof, fit, and next-step language instead of repeated placeholder patterns.
- Generic card labels were replaced with section-purpose labels across services, trust, results, editorial, FAQ, and CTA blocks.
- CSS now keeps screenshot and no-JS states visible, disables problematic `content-visibility` reservations for portfolio review, prevents horizontal overflow, and avoids awkward mid-word breaks.
- The pass preserves each site's existing inspiration pack, local assets, component families, section rhythm, and cross-site identity while improving presentation quality across all pages.
